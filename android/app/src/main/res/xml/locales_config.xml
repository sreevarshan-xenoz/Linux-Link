<?xml version="1.0" encoding="utf-8"?>
<!--
  Per-app language support (Tier-3 #17, Play table stakes). Only locales
  listed here get the system "Languages" entry for this app. Translations
  are not shipped yet (all UI strings are still hardcoded English), so this
  is the plumbing: add <locale android:name="xx"/> as each locale lands.
-->
<locale-config xmlns:android="http://schemas.android.com/apk/res/android">
    <locale android:name="en" />
</locale-config>
