<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Window theme only; Compose paints the app (LinuxLinkTheme). Kept in
         step with values-night so the pre-draw window never flashes the
         wrong lightness. -->
    <style name="Theme.LinuxLink" parent="android:Theme.Material.Light.NoActionBar" />
</resources>
