# AGENTS.md

This file provides guidance to the AI agent when working with code in this repository.

## Workflow rules

- **One logical change = one commit, and commit it before starting the next one.** A "logical change" is something a reviewer can describe in one sentence (one fix, one feature, one doc truth-correction) — that is the commit unit. Never bundle two independent changes into one commit just because they are sitting in the working tree together, and never hold changes back to be "efficient": batch commits hide which change broke what, and the whole point of committing as you go is that each step is independently verified and independently revertable.
- Gates before each commit, then commit immediately: the change must be fmt/clippy/test-clean on its own (see *Build / test / lint*), with its own conventional-commit message and scope: `feat(android): ...`, `fix(core): ...`, `docs: ...`. Stage files by name, so an unrelated dirty file can't ride along. Never push unless explicitly asked.
- Update the docs a change touches (README.md, CONTRIBUTING.md, docs/, `man/`) in that change's commit — the doc fix travels with the code it describes, but a doc that describes a *different* change gets its own commit.
- **Never commit anything sensitive, and verify the staged set before every push/commit — not just the file you edited.** No private keys, TLS/QUIC certs, pairing PINs, trust stores, tokens, `.env` files, real device identifiers/MACs/tailnet addresses, core dumps, logs, or database files. No agent- or editor-local state (`.qoder/settings.json`, `.qoder/sessions/`, `~/` — the stray SDK dir). Where the secrets actually live: `state_dir()` = `$XDG_STATE_HOME/linux-link` (`pairing.pin`, `trusted_devices.json`, `certs/`), i.e. **outside this repo** — never copy one in, and if a test needs one, generate a throwaway. Config examples ship as placeholders only (`.env.example`'s `JWT_SECRET` is a fake and must stay that way). Stage by name (never `git add -A`/`.`), run `git diff --cached --name-only` plus a `git diff --cached` skim as the last gate, and if a secret was ever committed treat the push as the leak: say so immediately rather than quietly rewriting history.
- Keep the *Current Status* section at the bottom accurate whenever project state changes — this file is the dev-agent context anchor for Linux Link.

## Build / test / lint

- Rust workspace members: `core`, `server`, `android/bridge`, `spike/iroh` (edition 2024; the spike is a throwaway research crate, `publish = false`).
- The bridge builds `linux-link-core` with `default-features = false, features = ["client"]`. Every change in `core` must compile and pass clippy under BOTH profiles:
  - `cargo clippy --workspace --all-targets -- -D warnings`
  - `cargo clippy -p linux-link-core --no-default-features --features client -- -D warnings`
- Tests: `cargo test --workspace`.
- Kotlin app: `cd android && ./gradlew assembleDebug`. Android SDK (Platform 37, Build-Tools 37, NDK 29) and Gradle wrapper are configured and buildable on host. The APK needs the bridge `.so` in `android/app/src/main/jniLibs/<abi>/` first (gitignored): `cd android/bridge && cargo ndk -t arm64-v8a -o ../app/src/main/jniLibs build`. See docs/development-setup.md §4.

## Gotchas

- `android/bridge` is a cdylib producing `liblinux_link_android_bridge.so`, loaded by `RustCore.kt`. JNI exports must match the Kotlin package exactly: `Java_dev_linuxlink_android_bridge_<Class>_<method>`.
- Edition 2024 requires `#[unsafe(no_mangle)]`, not `#[no_mangle]`.
- `main` is rustfmt- and clippy-clean as of 2026-09-24, so **`cargo fmt --all -- --check` and `cargo clippy --workspace --all-targets -- -D warnings` are gates, not baselines** — keep them green and do not add exceptions to hold them down (the pre-existing "7-error baseline" was a *build stop* in `core/src/streaming/{capture,capture_x11,streamer}.rs` that hid every lint behind it; it is gone). The durable trap from the debt era: a whitespace-only line makes rustfmt **abort before reporting anything** (internal error "left behind trailing whitespace" — it happened at `server/src/v2_multiplexer.rs:102`), so never read a fmt run that printed nothing as clean; check its exit status. Format or fix only files your change actually touches; never reformat wholesale.
- `cargo clippy --workspace` unifies features across members, so it catches client+server cfg conflicts that per-package checks miss. Run it after changing feature gates in `core`.
- Server runtime needs Linux desktop libs (PipeWire, Wayland, xdg-desktop-portal); the server *build* also links system FFmpeg (`ffmpeg-next` under the `encode` feature) — install the libav* dev packages, not just the `ffmpeg` binary.
- iroh lives behind the NON-DEFAULT `wan` feature (core); the Android bridge builds core with `client,wan`. WAN code paths are only compiled/tested with an explicit `--features wan` (e.g. `cargo test -p linux-link-core --features wan`).
- Hyprland 0.56's socket1 IPC write dispatchers (`dispatch`/`keyword`/`setoption`) are broken upstream (hyprwm/Hyprland#16224) — never use them. All window/workspace actions ride the uinput injection path (Super+N hotkeys, etc.). Read-side `j/…` queries work fine.
- VAAPI works on this box — the old "VAAPI init fails here" gotcha was WRONG. This is a hybrid Intel-iGPU + NVIDIA-dGPU laptop: `/dev/dri/renderD128` is the NVIDIA card (no VA driver → `Failed to initialise VAAPI connection`) and the Intel **iHD** driver that actually encodes H.264/HEVC lives on `/dev/dri/renderD129`. Both the in-process (`encoder_vaapi.rs`, C4) and the sidecar encoder now pick the node by trying each `/dev/dri/renderD*` (override with `LINUX_LINK_VAAPI_DEVICE`) instead of assuming `renderD128`. NVENC is the genuinely-blocked path: the 580.178.04 driver predates FFmpeg 9's NVENC API-13 requirement (13.1), so NVENC stays on the verified sidecar and any NVENC-specific work is untestable here.
- Every UI/transport feature is gated on `assembleDebug` + `lintDebug` (Kotlin) and both clippy profiles + `cargo test --workspace` (Rust), but **almost nothing has been run on a real device** — treat on-device behavior as unverified unless the bullet says otherwise. `docs/` carries the on-device verification checklist (commit 4469e83).

## Current Status (keep this up to date)

The detailed build journal lives in `git log` — one conventional commit per logical change, so status bullets here must stay at the milestone level, not the file/function level.

- 2026-09-22: **Phase-C UI/UX polish pass landed (plan: `~/.qoder/plans/tidal-inlet-warbler.md`; C1–C9, checklist §21) — then substantially fixed on first device run, see the next bullet.** The app got a real design system: `ui/theme/` tokens + `Prefs`-driven `LinuxLinkTheme` (system-follow light/dark, Material You on 31+, brand pine/amber fallback; themes.xml Light + new values-night kills the old dark-window mismatch), edge-to-edge everywhere and an **immersive auto-hide session** (system bars hidden in `RemoteScreen` only; one `chromeVisible` timer fades/reveals HUD + bottom row + action disc + shortcut bar together, sheets suspend it). Error UX is humanized (`ui/UiError.kt` maps raw Rust strings → friendly resources; raw goes to Logcat) with one snackbar host replacing 13 toasts and a centered connect/fail card with Retry + Re-pair. **No Material-icons-extended in the offline cache → `ui/Icons.kt` is a Canvas-glyph set** (`LlGlyph` value class of stable `DrawScope` primitives — superseded the same day by vector XML, see the device-remediation bullet below). Sheets/pickers became real `ListItem`s with radio/switch selected states (the "· current" suffixes are gone), pairing got progress + autofocus + reveal-PIN eye + animated success check, Home got an empty-state explainer + a new **Settings screen** (theme/dynamic-color/keep-screen-on/reveal-PIN/haptics/language/about) behind `AnimatedContent` routes in `MainActivity`, the siren now shows a full-screen Silence dialog, clipboard Clear confirms, `ui/Haptics.kt` (`View.performHapticFeedback`, Prefs-gated; `HapticFeedbackConstants.TOGGLE` resolved reflectively because the API-37 jar predates it) ticks the disc/mode/picks/chips, the shortcut bar regrouped to System|Workspaces chips, and the session notification ships real vector icons (`res/drawable/ic_stat_linux_link|ic_notif_disconnect|ic_notif_lock.xml`) + a channel description. Kotlin-only — no Rust/JNI/wire change; locale strings now **161/161/161** en/es/ta.
- 2026-09-22: **Phase-C remediated on-device (OPPO CPH2359, tailnet) — the first build the user actually ran, and it looked broken.** The headline bug was the icon layer: the runtime-Canvas `LlGlyph` set rendered as a tiny dot in the corner of its box on device, so the session action disc was a blank square and every sheet row lost its affordance. `ui/Icons.kt` now wraps 28 hand-authored `res/drawable/ic_ll_*.xml` Material vectors (24x24, black fill) drawn through `Image` + `painterResource` + `ColorFilter.tint` — the same framework path the notification icons already proved works here, and the `LlIcon` call sites are unchanged. Three more fixes only a real screen surfaced: `StatsHud` became a `FlowRow` (a six-item `Row` is wider than a portrait phone, which squashed the trailing metrics into a one-character-per-line column), session chrome became inset-aware and self-describing (light mode chip with its mode glyph, scrimmed circular Exit, bordered HUD panels, a dark floating `ShortcutBar` pill instead of a surfaceVariant sheet that vanished over video), and **any** touch on the stream now summons the faded chrome via `PointerEventPass.Initial` — the old design had only invisible 30 dp edge strips, which made the menu practically unreachable after the 6 s fade, and observing on the Initial pass means the summon tap still reaches the desktop instead of being swallowed. The app also shipped with no launcher icon (the launcher drew its default blob): `mipmap-anydpi/ic_launcher*.xml` is an adaptive icon on the brand pine plate, and its foreground had to be **filled** paths inside a scaled group — ColorOS refuses to rasterize stroke-only adaptive foregrounds (it substitutes a placeholder) and zooms the layer past the 108 dp safe zone. Verified live on device: session chrome, QuickSettings sheet, pickers, Home, Settings (theme radios apply and persist), Exit, and the launcher icon. Strings stay **161/161/161** en/es/ta (the `Mode:` prefix dropped from the chip label). Lint 19 → 18 findings (`MissingApplicationIcon` cleared, none added). Kotlin-only — no Rust/JNI/wire change.
- 2026-09-22: **Dead video link no longer looks like a healthy session.** A desktop-side teardown (server kick, sleep, Wi-Fi loss) left the phone showing the last frame under normal chrome with no way out: the bridge clears its `STREAMING_ACTIVE` flag only on a *client-initiated* `stop_streaming`, so nothing ever reported the drop and the HUD kept quoting a lifetime-average fps (the frame counter and start time are reset by that same explicit stop, and by nothing else). Fixed in Kotlin where the truth is visible — `H264Decoder`'s pull loop now treats a 10 s frame gap as a dead link (`keyint=60` guarantees packets far faster than that, so it cannot trip on a still desktop) and calls back through `RemoteDesktopView` onto the existing error card; Retry was verified end to end (kick → card in 10 s → Retry → new QUIC session in the server log → 25 s of live HUD with no false positive). The card also gained the warning glyph + centred copy the design called for. Device-confirmed alongside: the session notification ships the real `ic_stat_linux_link` monitor icon and iconed Disconnect / Lock-desktop actions with a channel description. **Open, NOT mine and actively being worked elsewhere:** the server journal shows the phone's v1 TCP control connection flapping (baseline ~4 reconnects/min, 18-36/min while the session sheets are used, each dying in ~20 ms on a `Broken pipe` in the clipboard/battery plugin), and the server's own `Kicked 1 stale session(s) due to reconnect storm` fires on it — the installed bridge `.so` is a mid-edit build of the uncommitted `core/src/protocol/{backoff,connection,v2}.rs` work, so re-check this against a clean tree before treating it as a shipping regression. Kotlin-only again — no Rust/JNI/wire change. **Superseded 2026-09-24:** the flapping is root-caused to the Android bridge opening a throwaway control connection per query, not to the in-flight `core/src/protocol/*` work — see the control-connection bullet below.
- 2026-09-24: **Phase 2 opened: the two headline "reported number that is not a measurement" defects are closed (`12167ce`, `ac9b5b9`, this commit).** The session HUD's `frame_drops` was a literal `0`; it is now the holes in the server's per-frame sequence numbers as the client's receive loop sees them — the only vantage that can know a frame never arrived whatever dropped it — measured as `highest - lowest + 1 - arrived` so an out-of-order stream completion corrects itself instead of accumulating phantom drops, with the window restarted on sequence 0 because a rebuilt encoder counts from 0 again (**2051**, and **2052**'s "delete it otherwise" is moot). Packet loss now moves the encoder's rate: `LossCeiling` folds into the live bitrate arbiter as `configured.min(relay_cap).min(preset_ceil).min(loss_cap)`, reading the transport's own cumulative `lost_packets`/`datagrams_sent` each 2 s tick — 20 % off per congested tick down to a 1 Mbit/s floor, 10 % back up per clean tick until the term is inert, with a 100-datagram sample floor so a still desktop cannot floor a healthy link (**2053**). Two things this surfaced that are worth knowing before touching bitrate code: the arbiter in `streamer.rs` is the *only* rate owner (the checklist's LAN/preset/loss items are the observable proof of every term in it), and the module's RTT controller was **dead** — `with_adaptive_bitrate` had no callers, so `AdaptiveBitrate`/`BitrateProfiles`/`AdaptiveBitrateMonitor` never saw a connection and the README's "RTT-based congestion control" described a no-op on top of unreachable code; it is deleted rather than revived, and 2821 (loss + RTT in one place) is now work against the arbiter. Loss response is unit-tested only — proving it needs a link that actually drops (checklist §11).
- 2026-09-24: **Phase 1 step 3 closed — one session record now carries all five percentile tails, measured by whoever can see them (`7a1623e`, `57094da`, `dba5d9e`, `ff1b526`, `6113600`).** The design decision was to ship **raw duration batches** from the device and let the *server* fold them through the same `core::metrics::Samples` reservoir that builds `enc_*`/`rtt_*`, instead of computing client-side percentiles that would not be comparable with the desktop's: `dec_*`, `rnd_*` and `e2e_*` in a line are the same code as `enc_*`, so tails across the whole chain can be read against each other. Two new `InputPacket` tags carry them (13 `LinkFeedback` = the phone's own rtt + received-side loss, 14 `ClientSamples` = kind + count + `u32` µs, exact-length checked; tags are never renumbered, unknown tags still hard-fail decode, and the two ends ship together with no compat shim). On the phone, `H264Decoder.kt` pairs each released output buffer with the feed that produced it — valid only because the stream is B-frame-free (R2#1) — clears the pairing on every codec open, MIME swap and resolution reconfigure rather than reporting a guess, caps a render gap at 1 s (longer is an idle desktop, not a slow panel), and bounds the pending-feed queue. Timing source is `elapsedRealtimeNanos/1000`, **not** `elapsedRealtimeMicros`: this SDK's android.jar does not expose the latter, the same trimming that already took `MediaCodecInfo.isDecoder`. Teardown of the bridge's sample path is non-blocking (a per-frame JNI push into a bounded batch, drained by the existing 1 s RTT tick), and `report_samples`/`SampleBatch::{new,clear}` are public so the phone's decoder is a first-class measurement source rather than a special case. Also fixed on the way: the server's connection monitor had a `data.len() == 16` pre-filter that silently swallowed any 16-byte tagged frame — dispatch is by tag again, which means an *older* client's untagged 16-byte stats buffer now logs one parse warning per second instead of being eaten unseen. `server/tests/session_record.rs` is the Phase-1 gate artifact: a real capture→encode→send→receive session over loopback QUIC that asserts the emitted record holds `rtt_*`/`enc_*`/`dec_*`/`rnd_*`/`e2e_*`, the link block and `phone_rtt`/`phone_lost` — so the client→server half of the chain is proven on this host and only the "a phone actually measures it" remainder stays device-owed. It is `#[ignore]`d (needs a desktop) and it caught two behaviors worth knowing: a session shorter than the server's 5 s path sampler emits no `rtt_*`, no link block and therefore no `phone_*` either (the client's readings hang off that block), and closing the QUIC connection underneath a running `StreamingClient::start()` does **not** end it — the cancel token does, which is the shape of the frozen-frame bug the phone's frame-gap watchdog exists for. Gates: fmt, workspace + `client`/`client,wan`/`wan`/`encode` clippy, bridge clippy `-D warnings`, `cargo test --workspace` (core 221 lib, 0 failed) and `--features wan`, arm64 `.so` with `nativeRecordSample` verified in it, `assembleDebug`, `lintDebug` at the 18-finding baseline with no finding in either changed Kotlin file. Docs (README sessions row, `man/linux-link.1`, checklist §Transport, plan step 3 + exit gate) now name which tail is measured where.
- 2026-09-24: **Phase 1 measurement half landed: the project now has tails, and a pinned way to regress them.** Before this there were zero occurrences of percentile/p95/histogram anywhere in the tree, so the class of bug this project keeps shipping — a change that hurts 5 % of frames — was structurally invisible. `core::metrics::Samples` is a bounded Algorithm-R reservoir (capacity 8192) emitting whole-millisecond `Summary{p50,p90,p95,p99,max}`, and `core/src/streaming/bench.rs` pins the workload it is measured against: 720p frames generated from `(width, height, index)` — a static diagonal gradient plus a moving 128×128 block with position-dependent pixels, so motion estimation gets a real residual — because storing 300 BGRA frames would be 1.1 GiB of git objects that anyone could quietly soften. `linux-link bench` (server `bench.rs`) turns that into a CI-readable contract: exit 0 pass / 1 regression / 2 usage / 3 **not comparable** / 4 unmeasured, where 3 is a first-class outcome — encode throughput does not travel between CPUs, and a baseline that disagrees on schema, backend, geometry, rate, preset, codec, frames, warmup or host is refused rather than judged. Two baselines are committed from this box (software p50 7 / p99 9; in-process VAAPI p50 4 / p99 6, actually on the iHD `renderD129`), and `--repeat` exists because the first recordings were contaminated at load average 6-8 (p50 11-18 ms) — best-of-7 recovered the quiet number, so the rule is keep-the-fastest, since foreign load can only slow an encode. Session telemetry gained the same tails (`enc_*`, `rtt_*`) and every session is retained as one JSON object per line for comparison. **Honest gaps, written into the plan:** decode/render percentiles were still open and phone-side at that commit (closed the same day by the client→server sample batch — see the five-tails bullet below), the "one desktop state, one link condition" claim is not true of a hosted runner, and the bench job's first outcome there will be `SKIP` until someone authors a baseline from its uploaded artifact. Gates: fmt, workspace clippy, `client`/`encode`/`wan` profiles, `cargo test --workspace` (~317) all green, plus the `#[ignore]`d real-encode bench test run explicitly.
- 2026-09-24: **Phase 1 step 5 (transport exposure) landed for the statistics that exist (a1590c3, 92e6465).** The step's framing — "surface what quinn and iroh already know" — is the whole scope, because path-MTU discovery and congestion control are library internals with no application surface: quinn already discovers the MTU and defaults to CUBIC behind a `TransportConfig` field, so the honest work is to *report* both, not implement either (the controller is deliberately left unswitched until a measurement says which tail it would move). `ConnectionStats` gained `congestion_events`/`cwnd_bytes`/`path_mtu`/`black_holes_detected` as `Option`, and the session recorder now emits a `LinkReport` per session: cumulative lost packets/bytes and datagrams/bytes moved, the **peak** congestion window (a window that collapsed and is still recovering must not read as uncongested), the number of times the selected path changed address (2155), and wall-clock seconds actually spent riding a relay (2157's "for how long"), with the relay interval closed at teardown so a session that ends still-relayed is billed to the end. **The asymmetry is real and tested on both sides:** iroh's connection statistics `Add<PathStats>` sums bytes and discards the per-path values outright, and noq's `path_stats()` is not re-exported, so those four fields are *absent* on a WAN record rather than zero — `key=0` therefore always means "measured zero", in the tab-separated line and in JSON. Open with the reason written into the plan: per-path split (2154), per-channel backlog (2152), datagram queue state (2153), connection-ID rotation (2156), direct-vs-relay delta computed from the records (2158), failure attribution by layer (2159), live `stats --json` (2160), a schema version on the session record (2162), and any phone-side delivery (2170) — 2170 still needs the desktop→phone direction; the client→server half landed the same day with the five tails. A live loopback session (`server/tests/session_record.rs`, `#[ignore]`d) now drives `run_pipeline` on this host and asserts the link block fills in, so what remains device-gated is only that a real phone produces the `dec_*`/`rnd_*` samples the test injects — still owed as checklist item §"Transport exposure". Gates: fmt, workspace clippy, `client`/`encode`/`wan`/`client,wan` profiles, `cargo test --workspace` (core 211, server 83) and `--features wan` all green.
- 2026-09-24: **Docs truth pass: the generated 2000-item roadmap is replaced by a verified 3000-item backlog plus an execution plan, and README/AGENTS now describe the tree that exists.** Every one of `779608f`'s ids was audited against source (seven per-range passes plus first-hand verification), and the result is `docs/roadmap-3000.md`: ids 001-2050 keep their numbers with an explicit DONE / PARTIAL / WRONG verdict per range (default ABSENT), and 2051-3000 are 950 new items derived from what the audit surfaced — U live defects, V observability, W testing/CI/release, X security, Y transport + message classes, Z capture/compositor, AA input, AB media, AC files/clipboard/notifications, AD product surface, AE ecosystem. `docs/roadmap-execution-plan.md` is the order, the exit criteria and the gates, and its **Phase 0 is this synchronisation** — the plan's central rule is that a claim in a doc must name the artifact that measures it, which is how `frame_drops: 0`, "adaptive bitrate" and "desktop audio" all survived review. `docs/roadmap-2000.md` deleted (superseded docs go, they don't get archived). Headline verified facts now written down: **282 Rust tests / zero Kotlin tests** (`android/app` has no `src/test` at all), **zero** occurrences of percentile/p95/histogram in `core/src`, `server/src` or `android/app`, no `benches/`, CI is one ubuntu job whose `cargo audit || echo` cannot fail anything, and **`main` has been red on the last six pushes** — every one at `cargo fmt --all -- --check` (96 hunks / 22 files at `90d92df`). README's "Phase 6 Complete — Release Ready" badge became an honest beta badge; the five fake `linux-link --status/--rollback/--uninstall` verbs moved to `scripts/install.sh` where they live, the non-existent `--config` flag is gone, and the ten real verbs are listed. Docs/README/AGENTS only — no code touched, nothing here needed a gate.
- 2026-09-24: **Green main (roadmap 2101/2102/2103), and the first honest measurement of the input-fairness claim.** `cargo fmt --all -- --check` passes, `cargo clippy --workspace --all-targets -- -D warnings` passes (also under `client`, `client,wan`, `wan`, `encode` core profiles), and `cargo test --workspace` is **279 passed / 0 failed / 13 ignored** — the first time all three have been true at once here, so `-D warnings` is now the gate rather than a baseline someone works around. Getting there: the documented "7-error clippy baseline" was a *build stop* in `capture.rs`/`capture_x11.rs`/`streamer.rs`, which is why nobody had ever seen the ~20 lints behind it in `server/`; the sweep also deleted two genuinely dead things (`PinEntry::phone_id`, `session_telemetry::MAX_LINE_BYTES`). `chaos_integration` no longer binds UDP 4716 (it fought the running user service for it) because `ChaosProxy::new` now binds, accepts port 0, and reports `local_addr()`; it also **counts every datagram as forwarded or dropped at decision time**, which replaced an assertion that inferred "chaos happened" from a 100-packet sample and therefore failed 2.65% of the time on its own RNG — the drop *rate* moved to a 100k-draw unit test where it is deterministic. **The finding: the project's stated input-fairness contract does not hold.** `video_flood_test` claimed to test it while connecting straight to the server and never through the chaos proxy it configured, so nothing was ever injected; routed properly at 10% datagram loss with a 1 MB/100 ms video flood, all 50 urgent packets do arrive, but their latency is **avg 2.2–3.8 s, max 6.3 s** against the "sub-300 ms" the test asserted (same flood at 0% loss: 12–89 ms). Quinn's `set_priority` does not bound the queue once retransmission pressure starts — nothing is lost, the urgent queue is simply seconds deep. The delivery half now asserts what is true, and the latency contract is a `#[ignore]`d test with these numbers in its reason so the gap stays measurable (`-- --ignored`); honest scope note: it drives **raw quinn streams**, not the production `streamer.rs` newest-frame/bounded-channel path, so a flood test against the real pipeline is still owed. Not done: the "green main" gate says *on a fresh clone and in CI* — commits are local, `main`'s CI is still red at the Format step until this is pushed, which is the user's call.
- 2026-09-24: **CI became a matrix (Phase 1 step 2) — written, not yet executed.** `.github/workflows/ci.yml` went from one job to four: `rust` (fmt, workspace clippy `--all-targets -D warnings`, release build, tests), `profiles` (a `fail-fast: false` matrix clippy-ing `core` under `client`, `client,wan`, `wan` and `encode`, and testing the last two), `android` (bridge clippy on host, then `cargo ndk -t arm64-v8a` to produce the gitignored `.so`, then `assembleDebug` + `lintDebug`), and `audit` with the `|| echo` removed so a dependency finding can actually stop a run. `release.yml` had two independent problems: the same `|| echo` pattern, and no apt dev packages — it links PipeWire/Wayland/libav*, so **its release binary has never been built by CI**, which is why `v0.1.0` is a tag with no artifact. The Android job's runner assumptions (NDK r29 discovered via `sdkmanager --list` rather than a rotting pinned string, Platform 37) are written from the repo's own build config and have **not** been observed on a runner: treat the first pushed run as a test of the workflow itself. Gates re-verified from a **fresh clone** at `195cafb`: fmt, workspace clippy, `cargo test --workspace` (285/0/13) and `cargo test -p linux-link-core --features wan` all green in that clone.
- 2026-09-24: **Phase 0 (state truth) is closed and `main` is green from a clean checkout.** fmt, `clippy --workspace --all-targets -D warnings` (plus the `client` / `client,wan` / `wan` / `encode` core profiles), `cargo test --workspace` and `cargo test -p linux-link-core --features wan` were re-run on a **fresh clone** at `195cafb` and all passed — the first time that has been true of this repo. Still unmet from that gate: *in CI*, which needs the push (the four-job matrix above has never executed and the pushed branch is red at `cargo fmt --check` until then) — the user's call, so "CI green" remains Phase 1 work. Phase 0 item 4 landed as **`core/src/capabilities.rs`: one generated source of truth** for negotiated protocol versions, transports, capture backends and codecs — every value is *read* from the constants and tables the wire uses (`capture_attempts`, `VideoCodec::ALL`, `AudioConfig::default()`, the ALPNs), feature-gated sections report *unavailable* rather than an empty list, and `linux-link capabilities` / `--json` / `--markdown` print it. `docs/capabilities.md` is that Markdown output, committed, and `server/tests/capabilities_doc.rs` fails while it is stale (verified by breaking it). Wire facts that had been duplicated across crates now exist once: `protocol::ALPN_V1_STREAM` (was four `"linux-link-stream"` literals), `v2::{V2_MIN_VERSION, V2_MAX_VERSION}` (both ends built their identity from their own `2`/`2`), and the mic relay's Opus rate/channels moved to `core::streaming::audio`. Also measured on the way and recorded rather than fixed: with the transport flood finally routed through the chaos proxy it configures, quinn stream priority gives **2.2-3.8 s average / 6.3 s max input latency at 10% loss** against the roadmap's sub-300 ms fairness contract, while delivering every packet — kept as an `#[ignore]`d test (ids 146-149 / 2449).
- 2026-09-24: **One shared v1 TCP control connection — the reconnect storm and the server's `Broken pipe` noise are the same bug, fixed in the bridge.** Root cause had nothing to do with the in-flight `core/src/protocol/*` work: every desktop *query* in `android/bridge/src/api.rs` opened its own throwaway connection to 1716/tcp, and the server's registry evicts by `device_id` (`state.rs::register_client`), so each `getBattery`/`get_monitors`/file-list evicted the live control registration and every *reply* left unread in the socket made the kernel send RST — which the plugins log as `Broken pipe (os error 32)`. The `StatsHud` 15 s battery poll is the ~4/min baseline, sheet/picker use the 18-36/min spikes, and the server's own `Kicked 1 stale session(s) due to reconnect storm` was firing on it. Now `connect_to_peer` pins the writer + peer address into `CONTROL_WRITER`/`CONTROL_PEER` and keeps a reader task feeding a broadcast (`INCOMING_PACKETS`); `control_request` subscribes **before** sending (the old `get_clipboard` subscribed after and could lose its own answer) and type-matches the reply, falling back to a dedicated connection only when there is no live session; the throwaway path half-closes (`shutdown()`) and drains to EOF so no RST is possible. Converted: clipboard, monitors, battery, privacy, audio, windows, exec, file-list, plus the four push-only calls (`send_clipboard`, `findmydevice`, `notification_reply`, `power`). Deliberately separate: `send_file`, because the desktop dials back our listener and the announcing socket must stay open. Bridge-only — no server, wire, or Kotlin change; reader teardown is guarded by `Arc::ptr_eq` identity and locks writer→peer in a fixed order so a dying session can neither clear nor dead-lock the replacement. Gates: bridge clippy `-D warnings`, bridge tests, core `client`-profile clippy, workspace clippy unchanged at the 7-error baseline, `cargo fmt`, arm64 `.so` rebuild, `assembleDebug` + `lintDebug` at the 18-finding baseline. **On-device confirmation still owed** (phone not attached): the journal must show no per-query `Incoming v1 TCP` and no `Broken pipe` WARN while sheets/pickers are in use. `cargo test --workspace` also throws `AddrInUse` in `chaos_integration::test_coffee_shop_wifi` — that test hardcodes UDP 4716, which the running user service owns; pre-existing in the in-flight test edits, not this change.
- 2026-09-20: Tiers 1–3 of the roadmap are fully implemented: native Kotlin client + Rust JNI bridge (~54 exports), streaming/encode/transport pipeline, absolute touch input, session shell, clipboard, single-window + per-monitor streaming, Hyprland workspace HUD, PIN pairing (TCP **and** QUIC paths), battery/siren/WoL relay/notification reply, privacy grab + blackout, audio control, PiP, roaming hardening, WAN (iroh) connect with LAN-first fallback, i18n (en/es/ta). **All pending on-device verification (no device attached).**
- 2026-09-22: Cross-network access + login autostart landed. The server now runs as a **systemd *user* service** (`linux-link.service` rewritten as a `graphical-session.target` unit, `~/.config/systemd/user/`) — a system unit can never work because capture needs the session's Wayland/PipeWire; live on this box, release binary auto-starts at login and the phone auto-reconnects through it. `scripts/install.sh` follows (user-unit paths, `sudo -u … systemctl --user` helper gated on a live user session, ExecStart sed'd to the real prefix, legacy system-unit cleanup on uninstall, user-unit status/summary text) and `aur/` now ships the unit under `/usr/lib/systemd/user/`; README §systemd Service documents the `--user` commands. QUIC identity persistence (item 3 above) is what makes autostart usable: TOFU pins survive the service restart. **Tailscale path verified on-device**: phone (oppo-reno8-5g, 100.82.170.9) → laptop (100.117.27.83) with a full PIN pairing + streaming session + pixel-exact taps over the tunnel (UFW already allows 1716/tcp+udp, 4716/udp from anywhere); the iroh WAN path still needs a real cellular test (phone's cellular was OUT_OF_SERVICE).
- 2026-09-22: **First real-device LAN verification (OPPO CPH2359 ↔ this Hyprland box): pairing, live video, and input injection all confirmed end to end**, with four fixes that only a real device could surface: (1) `input_injector.rs` — Wayland now picks uinput FIRST (enigo/XTEST moves nothing under Hyprland) and absolute motion rides a dedicated `ABS_X/ABS_Y`+`BTN_LEFT` pointer device built eagerly at startup (libinput silently drops MT touchscreens here; a lazily-created device loses the very first event); (2) `H264Decoder.kt` — the first decoder format change now always reports size even when unchanged, so the UI letterboxes/touch-maps against the real frame (was whole-container → Y clamped to 0); (3) QUIC identity persisted to `state/certs/` via `CertManager::load_or_create` (a regenerated cert made every TOFU-pinned phone reject LAN streaming after a server restart); (4) core — rustls `ring` provider installed explicitly (feature unification pulls aws-lc-rs + ring into the same process so auto-pick fails) and the PipeWire audio `Session` is kept alive for the audio task's whole lifetime (its Drop was cancelling the shared pipeline token immediately, killing audio at session start). Also `pair.rs` accepts `kdeconnect.identity` so PIN pairing can learn the phone deviceId, and the bridge shares the versioned `HANDSHAKE_HELLO/OK` constants (was sending the pre-versioning bare string). Tap-to-cursor mapping is pixel-exact through the letterbox; Esc via ShortcutBar lands.
- 2026-09-22: **App UI/UX refresh landed and device-verified (OPPO CPH2359, tailnet)** — checklist §19/§20. Session chrome: the bottom bar's button-wall became a mode chip + a floating "⋮" disc opening a grouped `QuickSettingsSheet` (Input / Streaming / Desktop — toggles, cycling value rows, cross-sheet navigation), and the stats/workspace HUD ghosts to 0.3 alpha ~6 s after link-up, tap to refocus (commits 1df9384 + docs). Home: the connect form is no longer the landing screen — a "Your saved computers" card list (`HomeScreen.kt`) over a new multi-host `HostStore` (JSONArray prefs, legacy migration, per-address cache sweep on remove), with Auto/Paired badges, per-card Wake + confirm-Remove, and an add-computer form whose ports/WoL fields hide behind an Advanced expander (auto-opened when editing a known host). 22 dead button strings replaced by 31 sheet + 10 home keys ×en/es/ta (128/128/128, grown to 161 by the Phase-C polish pass above). No Rust/JNI/wire change.
- 2026-09-22: **Latency fix — encoder Auto-detect now runtime-verifies NVENC and the C2 ladder tries VAAPI before software** (`encoder_detect.rs` + `encoder.rs`). Root cause of the high-latency sessions on this hybrid box: `ffmpeg -encoders` lists NVENC at *compile* time, so `probe_encoders()` reported it available → Auto picked NVENC → sidecar died (driver 580.x < FFmpeg 9's API-13) → silent software-x264 fallback. `probe_encoders()` now confirms NVENC with a throwaway 1-frame `h264_nvenc` encode before listing it, and the sidecar-failure branch in `VideoEncoder::new` inserts a `open_vaapi_verified()` rung (in-process C4 → verified sidecar) before `degrade_to_software`, so a failed NVENC pick still lands on hardware. **Verified live over the tailnet**: server log "Auto-detect: selecting VAAPI → in-process VAAPI (h264_vaapi) on /dev/dri/renderD129", phone HUD steady at fps 26 / rtt 28 ms / e2e 55 ms (was rtt 47-70 ms / e2e 158 ms on the software rung). Gates: 35 encoder tests incl. live VAAPI roundtrip, 176 core lib tests, client + encode clippy at baseline. No wire/JNI/Kotlin change.
- 2026-09-21: R4 (plan: `docs/rustdesk-research-roadmap.md`) — landed: A1 link-path reporting, A2 session outcome telemetry (`linux-link sessions`), A3/E5 relay bitrate floor + full-quality override, B1 wlroots `zwlr_screencopy` capture backend, B2 window-granular capture via `hyprland_toplevel_export_v1` (B1+B2 are the two items verified live on this Hyprland box), D1 server-enforced view-only mode, D3 one-time scoped PIN (`pair --grant`, lazy-expiry TrustStore grants), C1 codec capability negotiation (caps uni-stream `[0xFD,0x00,caps]`, server `allow_hevc` config gate default off, H264Decoder defers MediaCodec configure to first keyframe and sniffs avc/hevc from the NAL header), E2 phone-mic share (`InputPacket::Mic` tag 11 → server `mic_relay.rs` pw-loopback "Linux Link Mic" source — relay verified live on this box; Kotlin-side MediaCodec Opus encoder because the `opus` crate cannot cross-compile under cargo-ndk; RECORD_AUDIO + Android 14 `microphone` FGS type; 57 JNI exports), C2 runtime encoder fallback ladder (`SidecarEncoder::verify_startup` feeds probe frames for 600 ms — a broken-VAAPI child survives a bare exit-poll; failed hardware rung degrades to Software+H.264 in-process-first; encode-task supervisor rebuilds once on 5 Errs or a 2 s no-output stall, ends the session when the bottom rung is exhausted; `H264Decoder.kt` re-sniffs MIME per keyframe and swaps MediaCodec on a codec change with a phone toast), D2 desktop-side session visibility + kick (core process-global live-session registry, RAII dereg, `kick` closes the QUIC conn → normal teardown; server `live_sessions.rs` 1 s watcher: `live_sessions.json` mirror for `linux-link status`, notify-send per new session, TTL'd `kick` request file consumed by `linux-link kick <id|prefix|peer-IP|all>`; no JNI/Kotlin change). E3 compositor-true e2e probe (the header's capture→send age on the desktop clock + RTT/2, EWMA'd in the core client receive loop per session; the bridge's `e2e_latency_ms` HUD field now reads the real estimate instead of its old RTT proxy; excludes phone decode→panel — checklist §2b). WindowCrop wire is 25 bytes (u64 Hyprland address appended) — pre-B2 17-byte frames are rejected; both ends ship together, no compat shim. B3 capture backend ordering + config override (`start_capture_auto` now drives a data-driven attempt list from a pure `capture_attempts(backend, detected)` — Auto = [screencopy, portal] on Wayland / [x11] on bare X11; new `CaptureBackend` enum + `capture_backend` config.toml key pins a single backend so a runtime failure surfaces instead of silently falling back; threaded via `StreamingServer::set_capture_backend` at both LAN + WAN construction; no JNI/Kotlin change). D4 protocol versioning discipline (`NetworkPacket` gained an optional top-level `llVersion`, auto-stamped by `to_wire()` on every `kdeconnect.linuxlink.*` packet from an `LL_EXT_VERSION` const — native KDE types stay untagged; separate from the identity handshake `protocolVersion`; unknown fields already ignored (opaque `Value` body, no `deny_unknown_fields`) and now asserted by a regression test; the convention-violator `kdeconnect.notification-reply` renamed to `kdeconnect.linuxlink.notification_reply` — conformant `[a-z_]`, our namespace, rides the tag; both ends ship together, no shim; no new JNI exports). E1 pull-window-to-phone gesture (the R3#7 crop pipeline + R3#8 Workspace HUD stitched: `hypr_events.rs::state_packet` now embeds per-window crop geometry by reusing the windows plugin's `entries()`/`screen_box()` — `windows[]` gains `at`/`local_at`/`size`/`monitor_size`, body gains a `screen` box, `active` derived phone-side from `activeAddress`; `WorkspaceHud` renders a window-chip row, tapping one drives `RemoteScreen`'s new shared `pullWindow` lambda = the picker's exact `sendWindowCrop` path, re-tapping the highlighted chip returns to whole-desktop — the roadmap's "swipe away" is a re-tap since the chip row is horizontally scrollable; `desktopWindowFromJson` shared out of `WindowPicker`; no wire/JNI change, 57 exports; +1 live server test asserting the snapshot shape). C3 NVENC high-power pin (**hardware-gated — no NVIDIA on this box, only the pure command/parse logic unit-verified**): new `core/src/streaming/nvenc_power.rs` RAII `NvencPowerGuard` — on acquire queries GPU 0 max graphics clock (`nvidia-smi -i 0 --query-gpu=clocks.max.graphics`), enables persistence mode (`-pm 1`), locks clocks to the ceiling (`--lock-gpu-clocks=MAX,MAX`) to kill NVENC's adaptive-downclock latency jitter; Drop runs `--reset-gpu-clocks`. Every failure (no binary / `N/A` clock / refused lock) is a self-declining no-op that logs and NEVER issues a reset for a lock it didn't take. Held as an `Option<NvencPowerGuard>` on `SidecarEncoder`, acquired only when `hardware_encoder == Nvenc`, so it spans the encoder lifetime (session end + C2-degraded drop both restore); server-only `encode` feature, no wire/JNI/Kotlin change (57). E5-remainder HUD link-profile presets (`InputPacket::QualityPreset` tag 12, `[12, preset u8]`, control-plane latched in the monitor task, survives view-only; ids `PRESET_AUTO|QUALITY|BALANCED|ECONOMY` → a **bitrate ceiling** via pure `preset_bitrate_ceil(id, configured)` = `configured.min(band)` so native resolution is never dropped, only bandwidth — Balanced ≤5 Mbit/s, Economy ≤1.5 Mbit/s, Auto/Quality unbounded). Server: the iroh-only A3 relay-guard became an **always-on bitrate arbiter** (every transport, 2 s poll) computing `configured.min(relay_cap).min(preset_ceil)` and pushing `bitrate_tx` on edge; the encode task's bitrate-watch arm now **rebuilds the encoder at current frame res with the new rate** (reusing sticky `encoder_preferred`, C2 discipline; IDR first) instead of only logging — this also makes the A3 relay clamp materialise immediately (previously lazy until a resolution rebuild). **Honest axis scope: a live preset moves bitrate only** — resolution/fps are capture-loop params fixed at pipeline start, and a mid-session codec change needs the client to reconfigure MediaCodec (C1/C2 re-sniff); those stay session-start choices. Phone: bridge `api::send_quality_preset` + `nativeSendQualityPreset` → 58 JNI exports; `RemoteScreen` session bar a cycling "Quality: auto/max/balanced/economy" TextButton (blue off-Auto, error toast, re-armed on stream re-Up like view-only); en/es/ta strings; `RustCore.PRESET_*` const ids. checklist §11 rewritten for the arbiter + new §11b preset checks. Device behavior unverified.)
- API 37's android.jar removed `MediaCodecInfo.isDecoder()` and `MediaCodec.create{Encoder,Decoder}ByString` — HEAD's C1 codec-probe Kotlin broke when the SDK updated (android-37.2); use `createDecoderByType`/`createEncoderByType` and instantiate-probe instead of `isDecoder` scans.
- 2026-09-21: R4 E4 landed — compositor privacy shield (**visual half; verified live on this Hyprland box**). `core/src/streaming/shield.rs` (under `capture`) paints a full-perimeter "session active" frame via `zwlr_layer_shell_v1` — a transparent-centre, opaque-red `PrivacyShield::show()` ring (`Layer::Overlay`, all-edge anchor, `KeyboardInteractivity::None` so it is click-through and never steals the remote's input), so bystanders see the machine is remote-driven while the desktop centre stays visible and usable. Binds `zwlr_layer_shell_v1`/`wl_shm` defensively → `None` no-op on GNOME/KDE/headless/X11 (same posture as B1). Geometry is a pure unit-tested fn (`render_shield`/`is_ring`/`shield_thickness`); an `#[ignore]`d live test maps the layer and asserts the edge pixels through the B1 screencopy backend — this is why E4's visual shield is box-verifiable, not device-gated (my earlier grouping of E4 with C4/E6 was wrong). Wired into Tier-3 #15 in `server/src/privacy.rs`: `GrabState.shield: Option<PrivacyShield>`, shown on the first successful `EVIOCGRAB` and dropped (unmapped) on `release_input`/TTL expiry, so the phone's **existing "Privacy: on/off" toggle** drives it end to end — **no wire packet, no new JNI (still 58), no Kotlin change**. Honest limits: input-block stays `EVIOCGRAB` (this Hyprland does NOT advertise `zwp_input_inhibitor_v1` → the compositor-native invisible-block is intentionally not attempted; the grab already blocks); the ring is text-free (no font rasteriser pulled in); frame geometry fixed at `show()` (mid-session resolution change is acked, not re-rendered). No new clippy lints after fixing two (`Closed` unit-variant pattern, test `chunks_exact`→`step_by`). Gates: `cargo test --workspace` (core 172, server 68) + `shield::` unit tests, `-p linux-link-core --features wan`, core `client`+`client,wan` profiles clean, workspace clippy at the unchanged 7-error baseline, capture-profile clippy clean on `shield.rs`, bridge clippy `-D warnings`, ndk arm64 bridge build, rustfmt; shield live test run explicitly and green (edge pixels confirmed painted — not a silent skip). Docs: roadmap E4 ✅ + §6 order, plan.md E4 bullet, checklist §9 shield checks.

- 2026-09-21: R4 E6 landed — foldable/tablet dual-pane (device behavior unverified — no foldable attached). `android/app/.../ui/FoldableLayout.kt`: `rememberFoldingSplit` observes `androidx.window` `WindowInfoTracker.windowLayoutInfo` → maps a `FoldingFeature` hinge to a split axis (vertical→left|right, horizontal→top|bottom, none→single); `TrackpadDock` is a standalone indirect-control surface (drag→`sendMouseEvent` relative, tap→`sendMouseClick` press+release, two-finger→wheel) on the same `limitedParallelism(1)` ordered dispatcher as the in-view trackpad; `ControlDock` = trackpad over the desktop `ShortcutBar`. `RemoteScreen`'s root became a `BoxWithConstraints`: the session (video + HUD + WAN/status + control buttons) sits in a **stream pane** sized via `Modifier.fillMaxWidth(0.5f)`/`fillMaxHeight(0.5f)`+`align`, dock in the other half; the pane `Box` is a stable subtree so a fold changes only its modifier — the `SurfaceView`/decoder is NOT torn down. Auto-engages on a hinge **or** ≥600 dp window (tablet/unfolded); a "Pane: auto/dual/single" button cycles the override; shortcut bar relocates stream→dock while dual. `androidx.window:window:1.5.0` added to app deps (already in Gradle cache → builds `--offline`). **No Rust/JNI change (58 exports).** en/es/ta strings (`pane_*`, `trackpad_hint`). Gates: `assembleDebug` + `lintDebug` clean offline — no new lint findings (only pre-existing `context.resources` + Int-state advisories on RemoteScreen). Honest limits: everything here is Kotlin layout over the existing input bridge; hinge geometry uses the stable `FoldingFeature.orientation` but real crease-gap inset/resizable-multi-window behavior is untestable without a foldable. Docs: roadmap E6 ✅ + §6 order, plan.md E6 bullet, checklist §17.
- 2026-09-21: R4 C4 landed — in-process VAAPI encoder, **live-verified on this box** (`core/src/streaming/encoder_vaapi.rs`): hand-rolled raw-FFI `av_hwdevice_ctx_create → AVHWFramesContext(NV12) → av_hwframe_get_buffer → av_hwframe_transfer_data → h264/hevc_vaapi` pipeline (ffmpeg-next 9.0 has no safe hwdevice bindings), BGRA→NV12 `sws_scale` upload, forced-IDR via `AVFrame.pict_type`, RAII `Drop`. `VideoEncoder` gained a `VaapiInProcess` variant routing `HardwareEncoder::Vaapi` in-process-first → verified sidecar → software (C2 ladder). Device selection was the whole game: **it tries each `/dev/dri/renderD*` and keeps the first that opens** (this hybrid box encodes on the Intel iHD `renderD129`, not the NVIDIA `renderD128` the old sidecar hardcoded), with a `LINUX_LINK_VAAPI_DEVICE` override; `build_ffmpeg_args` now derives the sidecar node the same way. NVENC in-process stays driver-gated (580.x < FFmpeg 9 NVENC API-13). Server-only `encode` feature; **no wire/JNI/Kotlin change (still 58 exports)**. Gates: `cargo test --workspace` (core 177, server 68) incl. `test_vaapi_encode_roundtrip` (real 40-frame `h264_vaapi`, self-skips where no VA node exists) + 4 pure ordering/value unit tests; `-p linux-link-core --features encode` clippy clean on the new files, core `client` + `client,wan` profiles clean, `cargo fmt` on touched files; workspace clippy unchanged at the pre-existing 7-error baseline (`capture.rs`/`capture_x11.rs`/`streamer.rs`, none in encoder files).

- Next: the entire R4 execution order is complete for everything verifiable on this box — A1–A3/E5, B1–B3, C1–C4, D1–D4, E1–E6 all landed (E4 compositor shield visual half verified live; E6 foldable dual-pane shipped device-unverified; C4 in-process VAAPI verified live). Only genuinely-hardware-gated residue remains: **NVENC in-process** (needs an NVIDIA driver ≥ the FFmpeg 9 NVENC API-13 baseline; this box is 580.x) and **E6 foldable/tablet on-device behavior** (needs a real foldable — the dual-pane code shipped compile/lint-clean). R2#3 is now fully done (software *and* VAAPI in-process). Everything else needs a real device the user tests directly (all Android items ship device-unverified).
- Licensing rule (from the R4 research): RustDesk (AGPL-3.0) and Sunshine (GPL-3.0) contribute architecture/technique only, never code; scrcpy (Apache-2.0) and IronRDP (MIT/Apache-2.0) are the sole code-borrowable projects.
- Working tree routinely carries the user's parallel in-progress edits across core/server/bridge — never revert, stash, or reformat uncommitted work that isn't yours.
- `FIX_PLAN.md`, `ARCHITECTURE.html`, and `CHANGELOG.md` describe the Flutter-era design — historical reference, not current truth. (`plan.md` was updated 2026-09-19 to reflect the Kotlin client; its Flutter snippets remain under an explicit historical banner.)
