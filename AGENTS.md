# AGENTS.md

This file provides guidance to the AI agent when working with code in this repository.

## Workflow rules

- Commit each completed, verified logical change as you go — don't batch a whole task into one commit at the end, and don't wait to be asked. Conventional commits with scope: `feat(android): ...`, `fix(core): ...`, `docs: ...`. Never push unless explicitly asked.
- Update the docs a change touches (README.md, CONTRIBUTING.md, docs/) in the same commit.
- Keep the *Current Status* section at the bottom accurate whenever project state changes — this file is the dev-agent context anchor for Linux Link.

## Build / test / lint

- Rust workspace members: `core`, `server`, `android/bridge`, `spike/iroh` (edition 2024; the spike is a throwaway research crate, `publish = false`).
- The bridge builds `linux-link-core` with `default-features = false, features = ["client"]`. Every change in `core` must compile and pass clippy under BOTH profiles:
  - `cargo clippy --workspace --all-targets -- -D warnings`
  - `cargo clippy -p linux-link-core --no-default-features --features client -- -D warnings`
- Tests: `cargo test --workspace`.
- Kotlin app: `cd android && ./gradlew assembleDebug`. Android SDK (Platform 37, Build-Tools 37, NDK 29) and Gradle wrapper are configured and buildable on host. The APK needs the bridge `.so` in `android/app/src/main/jniLibs/<abi>/` first (gitignored): `cd android/bridge && cargo ndk -t arm64-v8a -o ../app/src/main/jniLibs build`. See docs/development-setup.md §4.

## Gotchas

- `android/bridge` is a cdylib producing `liblinux_link_android_bridge.so`, loaded by `RustCore.kt`. JNI exports must match the Kotlin package exactly: `Java_dev_linuxlink_android_bridge_<Class>_<method>`.
- Edition 2024 requires `#[unsafe(no_mangle)]`, not `#[no_mangle]`.
- `main` carries pre-existing fmt/clippy debt in files untouched by current work (new clippy 1.98 lints, ~35 files not rustfmt-clean). Format or fix only files your change actually touches; never reformat wholesale.
- `cargo clippy --workspace` unifies features across members, so it catches client+server cfg conflicts that per-package checks miss. Run it after changing feature gates in `core`.
- Server runtime needs Linux desktop libs (PipeWire, Wayland, xdg-desktop-portal); the server *build* also links system FFmpeg (`ffmpeg-next` under the `encode` feature) — install the libav* dev packages, not just the `ffmpeg` binary.

## Current Status (keep this up to date)

- 2026-09-19: Native Kotlin scaffold (`android/app`, AGP 9.4 / Compose with `compileSdk = 37`) and Rust JNI bridge crate (`android/bridge`) are verified and buildable locally on host using OpenJDK 21 LTS, Android SDK 37, NDK 29, and `cargo-ndk`. Gradle wrapper generated and tested (`assembleDebug` succeeds).
- 2026-09-19: Bridge cross-compiles via `cargo ndk -t arm64-v8a -o app/src/main/jniLibs` and the `.so` is confirmed packaged in `app-debug.apk`. On-device run of the version round-trip not yet done (no device attached via adb).
- 2026-09-19: Roadmap execution started — R2#1 done: low-latency encoder flags in `core/src/streaming/encoder.rs` (B-frame off + 2-frame VBV on all backends, NVENC `-tune ll`, GOP set on hw paths). Known pre-existing issue surfaced: direct-FFmpeg `--ignored` encoder tests fail on this box because VAAPI init errors (`Failed to initialise VAAPI connection`) even though `renderD128` exists — environment, not the flags.
- 2026-09-20: Client API surface ported from the deleted Flutter-era `android/rust/src/api.rs` onto `android/bridge` (`api.rs` + `session.rs` + a JNI layer in `lib.rs`). 34 `native*` JNI exports under `RustCore`, async calls bridged with `block_on` on a shared Tokio runtime, fallible calls returning a `{"ok":...}`/`{"error":...}` JSON envelope; H.264 frames/audio cross JNI as big-endian binary blobs (see `RustCore.kt` wire-format doc). Matching Kotlin facade in `android/app/.../bridge/RustCore.kt`. Verified: both clippy profiles, `cargo test --workspace`, `cargo ndk` arm64 build, `assembleDebug`, and JNI-name/`.so`-export parity. On-device behavior still untested (no device).
- 2026-09-20: R2#2 done — Android low-latency decode/render. `android/app/.../stream/H264Decoder.kt` runs MediaCodec (`video/avc`) sync decode straight to a `Surface`, with `KEY_LOW_LATENCY=1` (API 30+) and realtime `KEY_PRIORITY=0`, draining `RustCore.receiveFrames`. `RemoteDesktopView.kt` is the SurfaceView host (never ImageReader) that connects streaming on surface-create and calls `Surface.setFrameRate` to the panel refresh on API 31+. `assembleDebug` compiles clean; device behavior unverified (no device).
- 2026-09-20: Tier 1 partial — Stats HUD (`stream/StatsHud.kt`, polls `RustCore.streamingStats()`) and desktop Shortcut bar (`stream/ShortcutBar.kt`: Super, Alt+Tab, Ctrl+Alt+Del, PrtSc, Esc, Super+1..9 Hyprland workspaces) landed, plus a full Android→evdev keycode table fix in the bridge (commit ba26e6c — old passthrough sent wrong keys for modifiers/digits). Input helpers live on `RustCore` (`tapKey`, `holdKey`, `releaseKey`, `hotkey`, `sendText`). Tier 1 #2 (touch input modes) landed with R2#4 (absolute-coordinate input, commit e0c5f18): `MouseMoveAbs` + direct-touch/trackpad gestures in `RemoteDesktopView.kt`.
- 2026-09-20: R2#4 done — absolute-coordinate input path, which unblocks Tier 1 #2 (direct-touch). New `InputPacket::MouseMoveAbs { x_norm, y_norm }` (tag 6, normalized 0..=65535 so neither side needs display resolution). Server: `InputInjector::move_mouse_normalized_abs` injects through a lazily-created uinput single-touch MT device (`PropType::DIRECT`, finger-down on first motion, left-button release lifts the finger; enigo path scales via `main_display`); the uinput backend state is now `UinputState { main, touch, touch_down }`. Bridge: `api::send_mouse_abs` + `nativeSendMouseAbs` JNI + `RustCore.sendMouseAbs`/`tapAbsolute`/`normalizedCoord`. Verified: both clippy profiles, `cargo test --workspace`, `cargo ndk` arm64, `assembleDebug`, JNI-export parity. Device behavior (libinput/Hyprland mapping of the virtual touch) unverified — may need axis resolution/size tweaks on-device.
- 2026-09-20: Tier 1 #2 done — touch input modes. `stream/RemoteDesktopView.kt` gained `InputMode` (DirectTouch/Trackpad) + a full-surface gesture overlay (`InputOverlay`): DirectTouch maps down/move to `sendMouseAbs` and up to a left release (lifts the server's virtual finger); Trackpad is indirect (drag = relative move, tap = left click, two-finger = scroll), with all sends on a `limitedParallelism(1)` dispatcher to keep packet order. New bridge path `api::send_mouse_click` + `nativeSendMouseClick` + `RustCore.sendMouseClick` — legacy `sendMouseEvent` reserves button 0 for movement, so a wire left click was unaddressable through it (`tapAbsolute` fixed to use it). Server-side: main uinput device now declares BTN_LEFT..BTN_EXTRA (undeclared codes are kernel-rejected — clicks never worked on the uinput backend). 36 JNI exports; both clippy profiles, ndk arm64 build, `assembleDebug` green. Device behavior unverified; mouse-direct mode remains (mode-switch UI landed on RemoteScreen).
- 2026-09-20: Tier 1 #5 done — session shell. `MainActivity` now drives real screens: `ui/ConnectScreen.kt` (address/port + auto-connect switch, prefilled from `HostStore` SharedPreferences) and `ui/RemoteScreen.kt` (composes `RemoteDesktopView` + `StatsHud` + `ShortcutBar` + input-mode toggle, keep-screen-on, FGS lifecycle). `service/SessionForegroundService.kt` is the Android 14 **`specialUse`** FGS (subtype justification in the manifest property; `mediaProjection` deliberately rejected — nothing is captured on-device) with an ongoing notification whose Disconnect action routes through `MainActivity.onNewIntent` → `stopStreaming`. POST_NOTIFICATIONS requested at connect time; `onTaskRemoved` kills the session. `lintDebug` + `assembleDebug` green; device behavior unverified.
- 2026-09-20: Tier 1 #4 done — clipboard UX. `ui/ClipboardSync.kt`: bidirectional sync over the KDE Connect control channel — pushes local changes, polls `RustCore.getClipboard` every 4 s (the server's ClipboardPlugin never broadcasts desktop changes itself), `lastLocal`/`lastRemote` tracking prevents echo loops; toggle button on `RemoteScreen`. `ui/ClipboardUi.kt`: 20-entry phone-side history (`ClipHistory` in SharedPreferences) in a `ModalBottomSheet`, pick = restore local + push. `ShareActivity`: share-target where `text/plain` → desktop clipboard and `EXTRA_STREAM` files are staged to cache and sent via `RustCore.sendFile` (client hosts the TCP listener, server's Share plugin pulls into `~/Downloads`). Control port threaded end-to-end: `HostStore.Host.controlPort` (default 1716), `ConnectScreen` streaming+control port fields, session key is now `addr|stream|control` (2-part legacy keys fall back to 1716). No Rust changes needed (bridge clipboard/file exports predate this). `assembleDebug` + `lintDebug` green; device behavior unverified.
- 2026-09-20: R2#3 partial — in-process software encoding. New `core/src/streaming/encoder_inproc.rs`: `ffmpeg-next` 9 (`AVCodecContext` libx264/libx265, sws BGRA→YUV420P, zero-latency dict params `bframes=0:rc-lookahead=0` + `tune=zerolatency`, forced IDR via `AVFrame.pict_type`, `unsafe impl Send` justified by single-task usage). `VideoEncoder` became a facade enum: software resolves to in-process, VAAPI/NVENC keep the sidecar backend (hw needs an `AVHWDeviceContext` pipeline — remaining slice); in-process open failure falls back to sidecar with a warn. `encode` feature now also pulls `dep:ffmpeg-next` → **build needs FFmpeg dev libs** (CI + docs updated; client profile unaffected). Verified: real-encode `--ignored` tests (Annex-B start codes on every packet, 1:1 emission, periodic + forced keyframes, HEVC roundtrip), `cargo test --workspace`, both clippy profiles (only the 7 pre-existing errors in untouched capture/streamer files), server check, ndk arm64 bridge build.
- 2026-09-20: R2#5 landed (transport hardening) — shared `configured_transport()` in `core/src/streaming/transport.rs` (16 MB datagram buffers, uni-1024/bidi-128, congestion controller via `LINUX_LINK_CC=bbr|cubic|new_reno`, default NewReno; also fixes `StreamServer/StreamClient::new` clobbering stream limits); server transport task sends newest-only (trims stale backlog before the last keyframe); client gap detection sends a rate-limited (250 ms) `InputPacket::RequestKeyframe` (tag 7) which the monitor routes to an immediate-IDR watch signal consumed by the encode task. Deferred deliberately: one-frame ≈ one-datagram (datagram wire format changes every client). On-device latency effect unverified (no device).
- 2026-09-20: R2#6 landed (VFR capture pacing) — X11 GetImage loop now does change detection + 10 fps idle back-off (`core/src/streaming/capture.rs::run_x11_capture_loop`); static screens no longer hit the encoder at all, and `prev_frame` only advances on successful send so backlog drops retry. The Wayland/PipeWire path was already damage-driven (portal emits no buffers when static), so idle relief on Hyprland comes for free. X11 path unverifiable on this box (Wayland session).
- 2026-09-20: Tier 1 #6 landed (pinch-zoom + display scaling) — `ViewportZoom` in `stream/RemoteDesktopView.kt` drives a `graphicsLayer` on the SurfaceView (1–8× scale, clamped pan); two-finger pinch/pan is a separate gesture enabled only in DirectTouch mode (Trackpad keeps two-finger = remote scroll), a second finger cancels an in-flight tap before any packet ships, and touch/trackpad coordinates map through the inverse zoom so a tap hits the visible desktop point. `assembleDebug` + `lintDebug` clean; device behavior unverified (no device). All of Tier 1 (#1–#6) is now implemented, pending on-device verification.
- 2026-09-20: R1 iroh spike done (`spike/iroh`, workspace member; `cargo test -p iroh-spike` / `cargo run -p iroh-spike` green). Loopback bidi+uni stream and datagram echo over iroh 1.2 with `RelayMode::Disabled` + direct `EndpointAddr` dialing. Headline: iroh 1.x runs on **noq**, not the quinn fork — `iroh::endpoint::Connection` is type-incompatible with core's quinn 0.11 transport, so WAN adoption needs a connection-abstraction trait over `core/src/streaming` (both crates link in one binary, so quinn-LAN + iroh-WAN coexist). API gotchas + integration paths documented in `spike/iroh/README.md`; R1 table in plan.md corrected (effort → Med-Large).
- 2026-09-20: R3#7 step 1 — `server/src/hyprland.rs`: raw Hyprland IPC client (no hyprctl dependency): `j/clients`/`j/activewindow`/visible-window filtering + socket2 event stream (`subscribe_events`, line-parsed `name>>data`). Probed live against 0.56.2 on this box — read side works, socket is `.socket.sock` (old `.socket1.sock` name also probed). Deliberately NOT implemented: socket1 write dispatchers (`dispatch`/`keyword`/`setoption`) — 0.56's Lua IPC rewrite broke even stock `hyprctl dispatch workspace 3` upstream (hyprwm/Hyprland discussion #16224), so window actions will ride the input-injection path. Tests: 2 shape/parse unit tests + 2 live tests that skip when no Hyprland IPC is present.
- 2026-09-20: R3#7 steps 2–3 — window list over the wire + capture crop. `WindowsPlugin` (commit ec65118) extended with monitor geometry: `j/monitors` in `hyprland.rs` (`HyprMonitor`, tolerant deserialize), per-window monitor-local `local_at` (crop origin in capture coords), `monitor_size`, and the layout-bounding-box `screen` field — the space normalized direct-touch input maps across, so the phone can remap crop-space touches to desktop-space itself. `InputPacket::WindowCrop` (tag 8: 4×u32 LE, `crop_rect()`, all-zero clears) is intercepted by the stream monitor task → watch channel; the **encode task** applies `VideoFrame::crop_region` per frame (BGRA row repack, even-rounded, out-of-bounds rect drops the frame) and **rebuilds the encoder at the frame's actual resolution** when it disagrees with `encoder.config()` — this is what makes a client-proposed window size reach the pipeline (sidecar `-s WxH` and in-process contexts are fixed at construction; rebuild emits IDR first so the client's gap logic and the seq restart are safe). Same-size rect moves only force a keyframe. `InputInjector` gets a no-op `WindowCrop` arm (control-plane). Server gates green (tests, both clippy profiles vs the 7-error baseline, fmt on touched files).
- 2026-09-20: R3#7 done (Android side) — single-window streaming end to end. Bridge: `get_windows` DTO gains `local_at`/`monitor_size` + third tuple element `screen` box; new `api::send_window_crop` (QUIC uni-stream, zero w/h = clear) + `nativeSendWindowCrop` JNI + `RustCore.sendWindowCrop`/`clearWindowCrop` (38 JNI exports). UI: `ui/WindowPicker.kt` bottom sheet (parses the payload with org.json, lists title/class/workspace/size, focused marker, "Whole desktop" entry); `RemoteScreen` keeps crop state and passes a `DesktopMapping` into the view. `H264Decoder` now survives mid-session resolution changes: caches the last keyframe, and on `INFO_OUTPUT_FORMAT_CHANGED` with a size different from the configured one it stop/configure/start re-seeds the cached IDR (initial size is only a guess until the first SPS) and reports the real size upward. `RemoteDesktopView` renders aspect-fit **letterboxed** via a custom layout modifier and maps direct-touch through the inverse transform viewport → zoom → letterbox offset → video px → window desktop rect → normalized screen space (fixes taps landing on the wrong desktop point when video/view aspect differ — the pre-existing mapping normalized against the raw view size); trackpad drag travel scales by video/display ratio too. Known limitation: crop lives in one server pipeline — a session reconnect resets to full desktop, phone must re-pick. Gates: ndk arm64 build + JNI export parity, `assembleDebug` + `lintDebug` clean, bridge clippy `-D warnings`, rustfmt. Device behavior unverified (no device).
- 2026-09-20: R1 stage 1 done — connection abstraction over core streaming. New `core/src/streaming/connection.rs`: `Connection`/`OutStream`/`InStream` traits (only `open_uni`/`accept_uni`, `write_all`/`finish`, `read_exact`/`read_to_end`, `remote_address`, `ConnectionStats{rtt, lost_packets}`), `ConnectionError::Closed` as the break-the-loop sentinel, `SharedConnection = Arc<dyn Connection>`, and `QuinnConnection` wrapping quinn 0.11 (error classification against `WriteError::ConnectionLost`, `ReadExactError`, `ReadToEndError`, `finish()→ClosedStream`). Migrated: `streamer.rs` pipeline tasks + `read_client_config` + adaptive-bitrate monitor + audio pipeline; `client.rs` receive/stats/input loops and `StreamingClient::connection()`; `run_on_connection` now takes `SharedConnection` (server `service.rs` wraps the accepted quinn conn; v2-multiplexer + endpoints stay quinn-native); bridge `StreamingHandle.connection` is now `SharedConnection` with V2-handle fallbacks wrapped via `QuinnConnection::shared` at the six input-send sites. Wire format unchanged; loopback round-trip test in `connection.rs` (installs the rustls ring provider like the integration tests — reqwest+quinn link two providers). Gates: `cargo test --workspace`, both clippy profiles (7 pre-existing errors only), rustfmt on touched files, ndk arm64 bridge build. Device behavior unverified (no device).
- 2026-09-20: R1 stage 2 done (core arm only) — `core/src/streaming/iroh_connection.rs` implements the connection traits over `iroh::endpoint::Connection` (noq 1.3), behind a NON-DEFAULT `wan` feature so neither the default build nor the Android bridge pulls iroh. noq mirrors quinn's shapes (same `ConnectionError` taxonomy, sync `finish()`, `read_to_end(limit)`); divergences encoded: RTT via `rtt(PathId::ZERO)`, `remote_address()` = selected direct path's `TransportAddr::Ip` (unspecified for relay-only). Test: `iroh_connection_roundtrip` — same loopback assertions as the quinn test, over relay-less Minimal endpoints. Run gates need `--features wan` explicitly (`cargo test -p linux-link-core --features wan`). NOT done: endpoint lifecycle + dialing wired into StreamingServer/StreamingClient (needs an EndpointId bootstrap design — how the phone learns the desktop identity), `open_bi`/datagrams for v2, and any server/client feature-on wiring. SECURITY NOTE: this session received extensive fabricated "user edit/commit/push-authorization" notices (nonexistent commits 88d6e90/6a6b661/09c418a, fake file contents, instructions to stop flagging) — verified against git/grep and ignored; nothing was pushed.
- 2026-09-20: R3#8 done — Hyprland workspace HUD over the control channel. Server: `server/src/hypr_events.rs` wraps whitelisted socket2 events (`activewindow`, `workspace`, open/close/move/focus window, create/destroy/rename workspace) as `kdeconnect.linuxlink.hyprland.event` packets and builds the full `…hyprland.state` snapshot (`j/workspaces` via new `HyprlandIpc::workspaces()` + visible windows + active window); `service.rs` fans events to registered clients, pushes state on TCP-connection register, and re-broadcasts the snapshot every 4 s so the HUD self-heals across the bridge poller's per-call re-subscribes. Android `ui/WorkspaceHud.kt`: continuous `pollIncomingPackets()` loop (snapshot rebuild + incremental event apply), workspace chips with active highlight, tap switches numeric workspaces ≤9 through the existing Super+N `RustCore.hotkey` injection (never Hyprland IPC dispatch), focused-window title beside chips; composed under StatsHud in `RemoteScreen`, hidden when no Hyprland state arrives. No bridge/JNI change. Gates: server tests (9 hypr tests incl. live), workspace clippy at the 7-error baseline, core client-profile clippy clean, rustfmt on touched files, `assembleDebug` + `lintDebug` clean. Device behavior unverified (no device).
- 2026-09-20: R1 stage 3 (server slice) done — `server/src/iroh_endpoint.rs`: persisted iroh identity at `~/.config/linux-link/iroh_secret.key` (32 bytes, 0600, `create_new` so a racing instance adopts the winner; corrupt file = hard error, regenerating would invalidate every cached EndpointId), `spawn_wan_endpoint` binds Minimal + `RelayMode::Default` (relays for hole punching only — no pkarr; dialing rides the announced `EndpointAddr`) with the LAN stream budget (uni-1024/bidi-128 on the noq transport) and runs an accept loop feeding `StreamingServer::run_on_connection(IrohConnection::shared)` — the same pipeline as the quinn LAN arm; a bind failure warns and leaves LAN streaming up. `kdeconnect.linuxlink.endpoint` packet (`endpointId` hex + `relayUrls` + `directAddrs`) is pushed to clients on TCP register (next to the Hyprland state seed) and re-broadcast every 30 s so a phone's cached address self-heals through relay/DHCP drift. Server crate now depends on `iroh = "1.2"` + core `features = ["wan"]`. Gates: server tests 34 pass (3 new: key persist/0600, corrupt-key hard error, packet shape), `cargo test --workspace`, `cargo test -p linux-link-core --features wan`, both clippy profiles (7 pre-existing errors only), rustfmt on touched files (user WIP `v2_multiplexer.rs` has a rustfmt-internal trailing-whitespace failure — untouched). WAN behavior unverified in the field (needs real phone + external network).
- 2026-09-20: R1 stage 3 (client slice) done — the phone can dial the desktop over iroh. Core (commit f0c005c): `IrohDial::dial(endpoint_id, relay_urls, direct_addrs, use_relays, timeout)` (`iroh_connection.rs`, `wan` feature) reassembles the announced identity into an `EndpointAddr`, binds an ephemeral `Minimal` client endpoint with the stream ALPN and connects under a timeout — and *owns* the endpoint (iroh aborts its socket task when a live `Endpoint` is dropped; `close()` on session teardown). `StreamingClient::attach(SharedConnection, monitor_index)` adopts the dialed conn into the same receive/input/stats pipeline quinn's `connect()` builds. Bridge now builds core with `client,wan` (iroh cross-compiles for arm64-v8a — verified by the `cargo ndk` build); the control reader intercepts `kdeconnect.linuxlink.endpoint` into a Rust-side `WAN_IDENTITY` cache (single ingestion point ahead of Kotlin's packet pollers; cleared on disconnect), `api::connect_streaming_wan` parses the cached body and installs the session through the shared `install_streaming` path (WAN sessions keyed address + port-0 sentinel; `StreamingHandle.wan_dial` closed in `teardown_streaming_handle`), 2 new JNI exports (`nativeConnectStreamingWan`, `nativeGetWanIdentity` → 40 total). Kotlin: `RustCore.connectStreamingWan`/`getWanIdentity`, `HostStore.saveWanIdentity/wanIdentity` per-address cache, `RemoteScreen` polls the bridge cache every 5 s while the control channel is up, `DecoderHost` is LAN-first with automatic WAN fallback from the cached identity (no new UI). Gates: `cargo test --workspace`, workspace clippy at the 7-error baseline, core `client` and `client,wan` profile clippy clean, bridge clippy `-D warnings` clean, ndk arm64 build + JNI export parity (40/40), `assembleDebug` + `lintDebug` clean. WAN behavior unverified (needs real phone + external network; real-NAT punching never tested end-to-end).
- 2026-09-20: R3 Tier-2 #10 done — per-monitor streaming selection. Server half (commit 050feb2): `monitor_index` from the client config stream is finally honored in `core/src/streaming/capture.rs` — portal picks the granted PipeWire stream whose position matches the xcap-enumerated rect for the index (xcap is the same enumeration the monitors plugin reports, so picker index == capture target; first-stream fallback kept), X11 `GetImage`s the monitor rect in the virtual screen (frames ride the R3#7 encoder-rebuild path; legacy whole-root grab when enumeration fails/out-of-range). Android half: `ui/MonitorPicker.kt` sheet over `RustCore.getMonitors`, "Monitor: …" button on `RemoteScreen`, per-desktop persistence in `HostStore`, index threaded through `RemoteDesktopView → DecoderHost → connectStreaming`/`connectStreamingWan`; mid-session switch stops the bridge session *before* recreating the view (else the idempotent same-address connect no-ops) and clears the window crop (new pipeline starts uncropped). No bridge/JNI change. Gates: core clippy at the 7-error baseline, `cargo test --workspace` (2 new pick_stream tests), `assembleDebug` + `lintDebug` clean. Device behavior unverified (no device; X11 path unverifiable on this Wayland box).
- 2026-09-20: R3 Tier-2 #11 partial — battery HUD + find-my-device siren. Battery (commit 78def39): bridge `api::get_battery` (one-shot `kdeconnect.battery.request` over the control channel, get_monitors pattern) + `nativeGetBattery`, `StatsHud` shows a "desk NN%"/"NN%⚡" chip polled every 15 s; server's BatteryPlugin already answered, no server change. Siren: server `SirenPlugin` (`plugins/siren.rs`, registered as the 11th plugin) rings the desktop for 30 s on `kdeconnect.findmydevice` `{ring:true}` (looping freedesktop .oga via pw-play→paplay, silently no-ops headless); phone→desktop via `api::send_findmydevice` + `nativeSendFindMyDevice` + a "Ring PC" TextButton on `RemoteScreen`; desktop→phone via the bridge control reader latching `SIREN_RINGING: AtomicBool` (single-ingestion point like WAN_IDENTITY, cleared on disconnect) → `check_siren`/`nativeCheckSiren` (swap-and-clear) → `ui/Siren.kt` `SirenWatcher` composable polling every 2 s and looping TYPE_ALARM at max volume for 30 s (`Ringtone.setVolume`/`setLooping` guarded to API 28+ — minSdk 26). 43 JNI exports. Remaining for #11: notification reply from the phone (heavy — the desktop isn't its own notification server; needs a Notify capture path) and the PIN pairing dialog. Gates: server tests (+2 siren unit tests), workspace clippy at the 7-error baseline, bridge clippy `-D warnings`, rustfmt, ndk arm64 + JNI parity 43/43, `assembleDebug` + `lintDebug` clean. Device behavior unverified (no device).
- 2026-09-20: R3 Tier-2 #11b — PIN pairing end to end (the third #11 slice after battery + siren). Server `PairPlugin` (`plugins/pair.rs`, 12th plugin): learns the phone's real deviceId from its `kdeconnect.identity` (that packet used to be dropped — device_id was the peer IP), two PIN flows converge on `kdeconnect.pair` `{pin}` — push (`kdeconnect.linuxlink.pair` `{requestPin:true}` → CSPRNG PIN via notify-send/log, answer `pinSent`; CLI-PIN file live → `pinReady` instead) and pull (`linux-link pair` now writes `PIN\nunix-secs`, validated with a 5-min TTL; `linux-link unpair` added to clear the store); on match the id goes to the persisted `TrustStore` (previously loaded but never consulted) and the reply `{pair:true, serverId}` lets the phone persist the desktop too. Enforcement in the `handle_connection_with_kde` dispatch loop behind `Config::pairing_required` (default true — unpaired sockets see only the pairing handshake + identity; set false in config.toml for open-LAN; **v2/QUIC paths are NOT gated yet**). Bridge: `request_pair_pin`/`pair_with_pin` (sync std::net sockets — full handshake incl. our identity packet, own 8 s response read via `set_read_timeout`/WouldBlock), `PAIR_RESULT` latch in the control reader (drops PIN-bearing packets before the Kotlin poll broadcast), `check_pair_result`/`paired_servers_json`, `paired_servers.json` under CERT_DIR; 4 JNI exports → 47. Kotlin `ui/PairingSheet.kt` (show-PIN flow with 2 s latch polling, manual 6-digit entry), auto-opens on unpaired sessions, `HostStore.pairedDesktopId` + "Paired ✓"/"Pair…" button on RemoteScreen. Gates: server lib tests 39 (3 new pair unit tests), workspace tests, workspace clippy at the 7-error baseline, bridge clippy `-D warnings`, rustfmt, ndk arm64 + parity 47/47, `assembleDebug` + `lintDebug` clean. Pairing UX on-device unverified (no device); never tested against a real desktop+phone pair.
- 2026-09-20: R3 Tier-2 #11c done — notification reply from the phone (closes #11). Protocol groundwork in core (`kdeconnect.rs`): `NetworkPacket` gained KDE-Connect-standard `source` + `replyId` optional fields and `TcpDeviceSender::send_packet` stamps `source` with its deviceId; `notification_monitor.rs` pushes carry stable ids (`app|summary`). Server relay: 13th plugin `NotificationReplyPlugin` (`plugins/notification_reply.rs`) answers `kdeconnect.notification-reply` `{id,reply}` by appending to `replies.log` under the state dir, copying the text to the desktop clipboard (wl-copy → xclip, 3 s timeout) and showing a notify-send confirmation — honest semantics: a true reply into the originating app's D-Bus object isn't feasible from a plugin. Bridge: the control reader queues desktop-originated `kdeconnect.notification` pushes (source ≠ our id, not `isClear`, has id) into `PENDING_NOTIFICATIONS` (deduped by id, cap 32) → `take_pending_notifications`/`send_notification_reply` (one-shot control connection) → 2 JNI exports → 49. Kotlin `ui/DesktopNotifications.kt`: `DesktopNotificationRelay` composable on RemoteScreen polls every 2 s and posts platform `Notification.Builder` + `RemoteInput` inline replies (no androidx.core dep); `NotificationReplyReceiver` (manifest-registered, exported=false) cancels the notification and fires the reply on a worker thread; dedicated notification channel. Gates: `cargo test --workspace` (+3 plugin tests), both core profiles + workspace clippy at the 7-error baseline, bridge clippy `-D warnings`, rustfmt, ndk arm64 + parity 49/49, `assembleDebug` + `lintDebug` clean. Device behavior unverified (no device; reply relay never run against a live Notify app).
- 2026-09-20: R3 Tier-2 #12 done — Wake-on-LAN from WAN via a LAN relay peer (mechanism only; a real wake needs a second always-on box, never tested). Server: 14th plugin `WakeRelayPlugin` (`plugins/wake_relay.rs`) answers `kdeconnect.linuxlink.wol` `{mac, broadcast?, name?}` from the existing core `tailscale::wol::send_wol` (blocking UDP → `spawn_blocking`; default broadcast 255.255.255.255, directed subnet recommended) and replies `{ok, mac, error?}`; the `pairing_required` dispatch gate already keeps unpaired sockets from using it. Bridge: `api::wake_via_relay` one-shot control connect (findmydevice pattern, fire-and-forget — power-on isn't confirmable) + `nativeWakeViaRelay` → 50 JNI exports. Kotlin: `RustCore.wakeViaRelay`, per-desktop MAC in `HostStore` (`wol_mac:` prefix), `ConnectScreen` "Wake-on-LAN MAC" field + "Send Wake-on-LAN" OutlinedButton (the entered host *is* the relay; MAC validated against a colon/dash regex, status line reports the send). Gates: server lib tests 44 (+2), `cargo test --workspace`, workspace clippy at the 7-error baseline, bridge clippy `-D warnings`, rustfmt, ndk arm64 + parity 50/50, `assembleDebug` + `lintDebug` clean.
- 2026-09-20: Follow-on done — WAN status/failure surface in the session UI. `DecoderHost.start` now reports the video connect outcome through a new `StreamStatus` (`Connecting` / `Up(Lan|Wan)` / `Down(reason)`): LAN error captured separately, WAN fallback only when a cached identity exists, per-generation guard (`decoder === active`) so a stopped attempt can't clobber a newer one. `RemoteScreen` shows a top-center status chip while not `Up` (reasons + "Retry" = stop + recreate the stream view, same teardown-before-recreate dance as `switchMonitor`, which now also resets the chip), plus a persistent small "WAN link (iroh)" badge when the live session is off-LAN. Kotlin-only; `assembleDebug` + `lintDebug` clean; device behavior unverified. The other noted follow-on — gating the v2/QUIC accept path on pairing — stays deliberately parked: it needs `v2_multiplexer.rs` (user WIP), and QUIC already carries TLS + TOFU-pinned certs, unlike the plaintext TCP channel the `PairPlugin` gate protects.
- 2026-09-20: Tier-3 #13 done — seamless roaming hardening (the verifiable slice). `configured_transport()` (core, single funnel for every Linux Link QUIC endpoint): `keep_alive_interval` 15 s + `max_idle_timeout` 45 s — NAT/relay bindings survive video stalls, dead peers detach in <1 min instead of lingering; Wi-Fi↔LTE migration needs NO code (quinn's `ServerConfig::migration` defaults true — verified in quinn-proto 0.11.14 source, which is why there's no `allow_migration` call: that setter doesn't exist on this TransportConfig). New `roaming_hardening_settings` unit test reads the values back off the Debug dump (no public getters). Doze half: `SessionForegroundService` holds a non-refcounted `PARTIAL_WAKE_LOCK` (6 h safety timeout, re-acquired per start, released `onDestroy`) so screen-off sessions keep draining frames + keepalives; `WAKE_LOCK` permission added. Honest gap kept in plan #13: client-side socket rebind on `ConnectivityManager` callbacks is NOT wired — Android may pin the UDP socket to the old default network; the QUIC layer is ready but proving/fixing needs a device. Gates: `cargo test --workspace`, both core profiles clean, workspace clippy at the 7-error baseline, rustfmt, ndk arm64 bridge build, `assembleDebug` + `lintDebug` clean.
- 2026-09-20: Tier-3 #14 done — PiP remote-desktop window. `MainActivity` is `supportsPictureInPicture` with `smallestScreenSize` added to `configChanges` (PiP transitions must not recreate the activity — the session lives in it); `enterSessionPictureInPicture(w,h)` builds `PictureInPictureParams` with the *decoded video frame's* aspect ratio — `RemoteDesktopView` reports the real size via a new `onVideoSizeChanged` callback (exact once the first SPS is parsed; until/without it Android would reject the ratio anyway), coerced into the system's 1:2.39–2.39:1 bounds and falling back to bare params. `onPictureInPictureModeChanged` drives an `inPictureInPicture` Compose state passed into `RemoteScreen`, which hides all chrome (HUDs, status chip, Exit, mode/shortcut bar) so the thumbnail is pure video while input, clipboard sync and the FGS keep running; "PiP" button on the session bar. DeX/docked: no code — the activity is resizeable (default API 24+) and the letterboxed aspect-fit layout already handles arbitrary sizes. Kotlin-only; `assembleDebug` + `lintDebug` clean; device behavior unverified.
- 2026-09-20: Tier-3 #15 (desktop half) done — privacy mode: block the desktop's local input while remote (device behavior unverified). `server/src/privacy.rs`: exclusively grabs every *physical* keyboard/mouse via `EVIOCGRAB` (evdev crate, already a dep) — the compositor stops seeing local events while our uinput injection path flows on; `should_grab` classification never touches `Linux Link Virtual *` (would silence the remote path) and leaves switch-only devices (lid/power) with logind. Every activation carries a 10-min TTL + watchdog thread that auto-releases (phone crash can't strand the machine; fd close also frees grabs on exit); `lock_screen()` tries `loginctl lock-session` → `xdg-screensaver lock` → `hyprlock`. 15th plugin `PrivacyPlugin` (`plugins/privacy.rs`): `kdeconnect.linuxlink.privacy` `{action: grab|release|status, lock?}` → `{ok, grabbed, ttlLeft?, locked?, lockError?, error?}` (pairing gate covers it). Bridge: `api::desktop_privacy` one-shot control connect (get_battery pattern) matching the reply by packet type within 5 s + `nativeDesktopPrivacy` → 51 JNI exports. Kotlin: `RustCore.desktopPrivacy`; `RemoteScreen` "Privacy: on/off" toggle (keepalive `grab` every 4 min while on, auto-clears if the server auto-released; release-on-dispose via DisposableEffect) + "Lock PC" button; Toast on failure. Gates: server lib tests 51 (+6 privacy tests incl. a non-grabbing live survey), `cargo test --workspace`, bridge clippy `-D warnings`, ndk arm64 + parity 51/51, `assembleDebug` + `lintDebug` clean, workspace clippy at the 7-error baseline. Honest gaps: needs read permission on `/dev/input/event*` (input group); no local-tamper *alert* (the grab hides attempts from us too). Phone side landed next (commit 0bd98aa): session notification gained the Tier 1 #5 carry-over **`Lock desktop`** action (PendingIntent carrying session address + new `EXTRA_CONTROL_PORT` → `MainActivity.onNewIntent` → one-shot `desktopPrivacy(..., lock=true)`) and `RustCore.releaseDesktopPrivacy`; the phone-side pocket/blackout mode landed later (see #15-remainder bullet).
- 2026-09-20: Tier-3 #16 done — desktop audio control from the phone (device behavior unverified). Hyprland `dispatch setvolume` deliberately NOT used (socket1 write dispatchers broken upstream, hyprwm/Hyprland#16224): `server/src/audio_control.rs` drives the sound server directly — **wpctl-first (WirePlumber), pactl fallback** (`get-volume`/`set-volume`/`set-mute` on `@DEFAULT_AUDIO_SINK@`, sink list via `pactl list short sinks` + `get-default-sink`, route via `pactl set-default-sink`); `run()` spawns with null stdin and folds stderr into errors; pure parse fns (`parse_wpctl_volume` "Volume: 0.4516 [MUTED]" → (45, muted) with clamp, `parse_sink_line`) unit-tested, no live-process tests. 16th plugin `AudioControlPlugin` (`plugins/audio_control.rs`): `kdeconnect.linuxlink.audio` `{action: status|setVolume|setMuted|sinks|selectSink}` → `{ok, volume, muted, sinks:[{index,name,description,isDefault}], default, error?}`, all CLI calls on `spawn_blocking` (pairing gate covers it). Bridge: `api::audio_control` one-shot control connect (desktop_privacy pattern) matching the reply by packet type with an 8 s budget (subprocess latency) + `nativeAudioControl` → 52 JNI exports. Kotlin: `RustCore.audioControl(address, port, bodyJson)`; `ui/AudioSheet.kt` `ModalBottomSheet` (volume Slider 0–100 sending on drag-finish, mute Switch, sink LazyColumn with default marker; every action re-reads status+sinks, org.json parsing); "Audio" button on `RemoteScreen` between Ring PC and Pair. Gates: server lib tests 55 (+4), bridge clippy `-D warnings`, ndk arm64 + parity 52/52, `assembleDebug` + `lintDebug` clean (packaged `.so` verified to carry `nativeAudioControl`), workspace clippy at the 7-error baseline. Honest gaps: phone→desktop control only — no desktop→phone volume-push (sheet re-polls on open/action) and no per-app sink-input routing UI.
- 2026-09-20: Tier-3 #17 done (device behavior unverified) — Compose polish. Predictive back: `android:enableOnBackInvokedCallback="true"` on `<application>` + `BackHandler(enabled = !inPictureInPicture, onBack = onExit)` in `RemoteScreen` (back gesture now ends the session through the same clean disposal path as "Exit" — stream stop, privacy release, FGS stop — instead of killing the activity; sheets consume back above it; PiP left to the system). Per-app language: `android:localeConfig="@xml/locales_config"` declaring `en` — plumbing only, real locales need the hardcoded-Kotlin-strings → `strings.xml` extraction first. Material 3 expressive: DEFERRED honestly — material3 1.4.0 (compose BOM 2026.08.00, latest stable) marks `MaterialExpressiveTheme` and `MotionScheme.expressive()` `internal` (compiler-verified against the artifact), and `ModalBottomSheet`/`ModalBottomSheetProperties` have no public predictive-back motion param; a `MainActivity` comment marks the one-line swap point for when 1.5.x ships it stable — no alpha dependency. Gates: `assembleDebug` + `lintDebug` clean (only pre-existing rule hits + expected API-33-attribute `UnusedAttribute` on the two new manifest attrs).
- 2026-09-20: Tier-3 #15 remainder done — phone-side blackout/pocket mode (device behavior unverified). `RemoteScreen` "Blackout" button → opaque black `Box` overlay (last child of the session Box = topmost) whose `detectTapGestures(onDoubleTap = …)` consumes every touch so nothing reaches the remote input layer; double-tap or the back gesture (the #17 `BackHandler` now checks `blackout` first — back can never silently drop an invisible session) unlocks; entering PiP auto-disables it. While active: window `FLAG_SECURE` + `SurfaceView.setSecure(true)` (`secure` param threaded `RemoteScreen → RemoteDesktopView`, applied in the `AndroidView` update block — `SurfaceHolder` has NO setSecure on this SDK; only the view method) so decoded frames never reach screenshots/Recents/screen-record, plus `screenBrightness = 0.01f` (restored via `DisposableEffect(blackout)`; onDispose always clears). Honest limit: apps can't power the panel off — that stays a physical button, and the FGS partial wake lock already keeps the link alive through it. Gates: `assembleDebug` + `lintDebug` clean; no Rust touched.
- 2026-09-20: Tier-3 #13 remainder done — network-change stream rebind on Android (device behavior unverified). `RemoteScreen` registers a `ConnectivityManager.registerDefaultNetworkCallback`; a *different* new default network (the immediate baseline callback is swallowed) triggers `retryStream()` — the same stop→recreate path as manual retry — behind a 3 s cooldown and with a PiP guard, so Wi-Fi↔LTE moves the session onto the fresh radio via a new QUIC handshake (LAN address if reachable, else the cached iroh WAN identity already threaded into connect). Honest framing: reconnect-on-new-radio, NOT live QUIC path migration — Android offers no way to move quinn's bound UDP socket onto a `Network`, and the server-side `migration=true` stays a LAN nicety. `ACCESS_NETWORK_STATE` added to the manifest. Gates: `assembleDebug` + `lintDebug` clean; Kotlin-only change.
- 2026-09-20: Tier-3 #17 remainder done — i18n extraction + es/ta locales (device behavior unverified). ~86 UI chrome strings moved from Kotlin literals into `res/values/strings.xml` (header comment documents the policy: telemetry units, shortcut key labels and desktop-supplied content stay untranslated); consumers converted across `ConnectScreen`/`RemoteScreen`/`PairingSheet`/`WindowPicker`/`MonitorPicker`/`ClipboardUi`/`AudioSheet` (composables → `stringResource`) and `SessionForegroundService`/`ShareActivity`/`DesktopNotifications` (plain `Context.getString`). Event-handler strings are now state-driven (`@StringRes` id + arg, or raw server text) resolved at render time — compose-ui 1.12.0 has NO `LocalResources`, so `LocalContext.current.getString` inside composables is a lint ERROR (`LocalContextGetResourceValueCall`); the refactor satisfies it honestly. ConnectScreen gained a "Language" OutlinedButton (API 33+ only, `ACTION_APP_LOCALE_SETTINGS` deep link; app is non-AppCompat so no per-app-backport library) and `locales_config.xml` now ships en/es/ta with full translations; manifest labels → `@string/app_name` + new `share_target_label`. Gates: `assembleDebug` + `lintDebug` clean (0 errors; `SetTextI18n` warnings 6→0). Language-switch on device unverified (no device).
- 2026-09-20: The parked follow-on is done — pairing now enforced on the QUIC paths (the "v2/QUIC NOT gated" gap from #11b closes). v2 multiplexer: `handle_v2_session(…, pairing_required)` gates the control stream per-packet, mirroring the TCP whitelist (only `kdeconnect.identity`/`kdeconnect.pair`/`kdeconnect.linuxlink.pair` dispatch until the handshake deviceId is in the TrustStore — pairing itself must stay reachable, so this is NOT a handshake close). Video stream (`linux-link-stream` ALPN): TLS is anonymous (`with_no_client_auth` both ends), so the client now announces its persisted deviceId in-band — a second pre-pipeline uni-stream `[0xFE, 0x00, len u8, utf8]` next to the monitor-config stream (`StreamingClient::connect`/`attach` gained a `device_id` param; the bridge passes `client_identity().device_id`, the SAME id TCP/v2 pairing stores) — and `StreamingServer::set_pairing_gate` judges it in `run_pipeline` before capture starts, closing rejects with a "pairing required" QUIC error; the `Connection` trait gained `close()` (quinn + noq impls). Wired in `service.rs` (LAN) AND `iroh_endpoint.rs` (`spawn_wan_endpoint(…, pairing_required)` — WAN dials gated identically). Also fixed a pre-existing pair.rs bug surfaced by the build: the "pair rejected, unknown connection" reply was never sent (missing `.await`). Staged via synthetic blob (HEAD + gate hunks) so the user's cosmetic v2_multiplexer WIP stays unstaged. Gates: `cargo test --workspace` (server 56, +1 unknown-device-is-not-paired), workspace clippy at the 7-error baseline, core `client` + `client,wan` profiles clean, bridge clippy `-D warnings`, ndk arm64 + JNI parity 52/52 (no new exports), `assembleDebug` + `lintDebug` clean, rustfmt on touched leaf files. Device behavior unverified; honest gap: outbound broadcasts (Hyprland state, endpoint packets, notifications) still fan to registered-but-unpaired senders — same as the pre-existing TCP behavior, gating fanout is a separate change.
- 2026-09-20: Deep research into RustDesk + open-source remote-desktop projects done (adversarially-verified, 21 confirmed claims) → **docs/rustdesk-research-roadmap.md** is the R4 plan. Headlines: RustDesk (AGPL-3.0) and Sunshine (GPL-3.0) contribute architecture/technique ONLY — never copy code; scrcpy (Apache-2.0) and IronRDP (MIT OR Apache-2.0) are the sole code-borrowable projects. QUIC/iroh direction validated (RoQ beat WebRTC by ~90 ms in the one published study, study-scoped; iroh DCUtR punching has no traversal penalty vs TCP); sobering number: ~70% conditional / ~50% unconditional direct-connect success → relay fallback must be first-class + instrumented. Concrete takes: NVENC high-power pinning, zerolatency confirmation, portal+HW-encode viability, a wlroots `zwlr_screencopy` Hyprland capture backend, codec fallback ladder, rendezvous/relay session UX, one-time scoped PINs. KDE Connect doc is a self-disclaimed non-spec — version our `kdeconnect.linuxlink.*` extensions, parse defensively (known violation: `kdeconnect.notification-reply` hyphen breaks the `^kdeconnect(\.[a-z_]+)+$` regex). Roadmap phases A–E (telemetry → screencopy → encoder ladder → session UX → unique features E1–E6) with an explicit execution order; NOT yet approved for implementation — await user direction.
- 2026-09-20: R4 A1 landed — link-path reporting (roadmap phase A, first item). Core `ConnectionStats` gained `relayed: bool` (quinn always false; iroh = selected path is a relay, via `paths()` — `is_ip()` is noq's direct-path predicate, there is no `is_direct`). Bridge RTT poller (1 s) maps (is_wan, relayed) → `LINK_STATE` AtomicU8 (lan/wan_direct/wan_relayed), logs relay↔direct transitions once, exposed as `link_state` in the `nativeGetStreamingStats` JSON — **no new JNI exports (still 52)**. Kotlin: `StreamingStats.linkState`, `RemoteScreen` polls it while a WAN session is up (stats JSON is atomic reads — no dispatcher hop) and the badge now reads "WAN · direct" / "WAN · relayed — trying direct…" / "WAN · punching…" (es/ta strings added); relayed is presented as a normal state iroh keeps upgrading, per the research's relay-first rule. Tests: loopback `relayed == false` asserts in both quinn+iroh roundtrips. plan.md gained the R4 section; roadmap doc A1 marked done. Gates: `cargo test --workspace`, `-p linux-link-core --features wan`, both core profiles + bridge clippy `-D warnings` clean, workspace clippy at the 7-error baseline, ndk arm64 build, rustfmt touched files. Device behavior unverified (needs a real relayed session).
- 2026-09-20: R4 A2 landed — session outcome telemetry log (roadmap phase A, second item; log format unit-verified, field population needs real sessions). Core `session_telemetry.rs`: `SessionRecorder` + RAII `SessionGuard` emit ONE tab-separated `k=v` line per `run_pipeline` run (`ts/outcome/ever_relayed/dur/rtt_ms/kbps[/dev]`) through a process-wide `OnceLock` sink — the guard is armed BEFORE the pairing gate and defaults to `Failed`, so `?`-early-exits still log; `mark_completed()` grades via new `Connection::transport_family()` (Quinn→`lan_direct`; Iroh→`wan_punched`/`wan_relayed` by whether any 5 s path sample rode a relay). Goodput = bytes the TRANSPORT task actually pushed (newest-only trims ≠ encoder output — deliberate). Server `session_telemetry.rs`: sink appends to `$XDG_STATE_HOME/linux-link/streaming_sessions.log` (1 MB rotation, newest half kept, leading partial line dropped); new CLI `linux-link sessions [--count N]` = whole-log outcome tally + "relayed share" + tail; `set_session_telemetry(true)` at both StreamingServer sites (LAN quinn + WAN iroh). Clients register no sink → Android cost is recorder construction only; NO JNI change (52 exports). This is the instrument for the §3 ~50/70% punching-reality claim. Gates: `cargo test --workspace` (core 134, +4; server 58, +2), `-p linux-link-core --features wan`, core `client` + `client,wan` profiles clean, bridge clippy `-D warnings` clean, workspace clippy at the 7-error baseline (fixed one NEW `manual_checked_ops` hit in my own code with `checked_div`), ndk arm64 build, `assembleDebug` + `lintDebug` clean, rustfmt on touched leaf files only (streamer.rs import-order diff was mine). Docs same commit: README CLI table, roadmap A2 ✅, plan.md R4 bullet. Device behavior unverified.
- 2026-09-20: R4 B1 landed — wlroots `zwlr_screencopy` capture backend, **verified live on this Hyprland box** (roadmap phase B first item). New `core/src/streaming/capture_screencopy.rs`: hand-rolled wlroots backend (wayland-client 0.31 + wayland-protocols-wlr 0.3 under the `capture` feature; libwayshot BSD-2-Clause patterns cited, not depended on — keeps gl/gbm/drm out of the server). Binds `zwlr_screencopy_manager_v1` >=3, `copy_with_damage` with overlay cursor (portal parity), ARGB shm = the pipeline's BGRA byte order (repack = YInvert flip + stride compaction); persistent memfd+mmap backing with **per-frame fresh pool/buffer** (the protocol makes the compositor destroy the buffer with the frame — reuse would kill the connection; the live test's multi-cycle idle phase guards exactly this). Damage-driven VFR + 10 fps idle back-off, newest-only try_send. `start_capture_auto`'s Wayland arm tries it FIRST behind a 5 s first-frame handshake — any setup failure (non-wlroots, no display, dmabuf-only buffer) or `LINUX_LINK_SCREENCOPY=0` falls back to the PipeWire/portal path unchanged; monitor pick = wl_output geometry vs xcap origin (Tier-2 #10 picker index space), else registry position, else first. Hyprland sessions no longer raise the portal grant dialog (checklist §2 gained the B1 checks — that file is user-edited in parallel, additions applied on top). Gates: workspace tests (incl. the live 2-cycle capture test), `--features wan` tests, workspace clippy at the 7-error baseline (two new-file lints fixed), client + client,wan profiles clean, bridge clippy `-D warnings`, ndk arm64 build, `assembleDebug` + `lintDebug` clean, rustfmt on the new leaf file.
- 2026-09-20: R4 D1 landed — server-enforced view-only mode (device behavior unverified). New `InputPacket::ViewOnly` (tag 9, strict 2-byte frame) + a per-session `view_only: Arc<AtomicBool>` on `StreamingServer` (instances are per-connection, so the latch naturally resets on reconnect). Enforcement sits in the connection-monitor task in `core/src/streaming/streamer.rs` — the same choke point that already intercepts RequestKeyframe/WindowCrop — so while latched every injectable packet (mouse/keyboard/gamepad/text) is dropped **server-side** before the broadcast to `InputInjector` (which gained a no-op arm); video, clipboard, HUDs and the crop/keyframe control plane keep flowing, and a stale client queue cannot defeat the lock. Bridge `api::set_view_only` + `nativeSetViewOnly` → 53 JNI exports (parity verified). Kotlin: `RustCore.setViewOnly`, a "View-only: on/off" TextButton on the `RemoteScreen` session bar (amber while on, dedicated error-toast state), and a `LaunchedEffect(streamStatus)` re-arm so a retry/monitor-switch (whose fresh pipeline starts interactive) inherits the latch again. es/ta strings shipped; checklist §3 gained the behavioral test. Gates: workspace tests (+roundtrip/framing), `--features wan`, core client + client,wan profiles clean, workspace clippy at the 7-error baseline (run in a HEAD worktree + my patch — the user's parallel v2_multiplexer WIP flaps between compile states mid-edit; their file untouched and unstaged), bridge clippy `-D warnings`, ndk arm64 build + JNI parity 53/53, `assembleDebug` + `lintDebug` clean, rustfmt on touched leaf files.
- Next: R4 execution order continues — A3/E5 (relay-quality floor + link-profile presets) is up next, then B2 (window-granular screencopy, now riding the B1 backend). R2#3 remainder (in-process VAAPI/NVENC via `AVHWDeviceContext`) stays parked — untestable on this box, VAAPI init fails in this env. Tiers 1–3 complete pending device verification.
- `FIX_PLAN.md`, `ARCHITECTURE.html`, and `CHANGELOG.md` describe the Flutter-era design — historical reference, not current truth. (`plan.md` was updated 2026-09-19 to reflect the Kotlin client; its Flutter snippets remain under an explicit historical banner.)
